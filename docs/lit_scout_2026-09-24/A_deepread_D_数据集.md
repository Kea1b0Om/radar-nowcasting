# 附录 A（D_数据集）：精读论文逐篇零件表（1 篇）

说明：每篇先由一个代理通读 arXiv 全文 PDF（从 arXiv 官方 GCS 批量桶取得）拆零件，再由另一个代理回到原文逐个核对数字、表号、口径、越协议与已关闭族判断，下面是**核查后的修正版**。“消融证据”列中的数字均来自 PDF 原文表格/正文（写明表号与页码）；标“图读数”的是从图上读的近似值，不能当表格数字引用。评分 0–3 为“值得上机”。任务标签：A1 融合占位 / A2 融合零件 / A3 叙事 / B 会议 / C 小数据泛化 / D 数据集数字 / E 雷达外推方法 / F 视频·时空预测 / COLL 撞车核查。每篇只出现在其主任务文件里（主任务按 A1>COLL>A2>A3>E>D>C>F>B>G 取第一个）。

## 索引

| # | arXiv | 标题 | 全部任务 | 评分 | 撞车 | 代码 |
|---|---|---|---|---|---|---|
| 1 | [2608.20980](#p260820980) | A Critical Audit of Spatiotemporal Forecasting Benchmark Datasets and Baselines | B,D,F | 1 | 无 | 无 |


---
<a id="p260820980"></a>
### A Critical Audit of Spatiotemporal Forecasting Benchmark Datasets and Baselines

- **ID**：arXiv 2608.20980　**Venue**：arXiv 预印本（v1，2026-08-21，cs.LG），未见录用信息
- **Venue 依据**：p1 页脚只有"Preprint."，侧栏是"arXiv:2608.20980v1 [cs.LG] 21 Aug 2026"；全文找不到任何会议或期刊录用注记。
- **代码**：无（无。论文里唯一的 github 链接是依赖库 https://github.com/Nixtla/statsforecast（p10 参考文献），不是作者的代码；全文也没有开源承诺。用 GitHub 仓库搜索试了 4 组关键词（标题、SARIMA residual GCRN chickenpox、chickenpox pedalme wikimaths sarima 等），都是 0 条结果。）
- **阅读层级**：全文PDF　**核查结论**：corrected　**任务**：B,D,F
- **一句话**：这篇审计的对象是 5 个图结构时空预测基准（Chickenpox、PedalMe、WikiMaths、METR-LA、PEMS-BAY），结论有四条：(1) 空间信息弱，主要靠节点自身的时间相关；(2) PyG-Temporal 里 Chickenpox 和 PedalMe 的 loader 默认给一阶差分数据，这会伤害泛化；(3) SARIMA 是被低估的强基线；(4) 让 GNN 学 SARIMA 的残差（同时把 SARIMA 预测当协变量），在交通数据上 6 个指标里有 5 个最好，但交通数据上的 SARIMA 用的是作者自称 unfair 的不限阶协议。全文不涉及雷达、SEVIR、降水、CSI，全文 grep 已确认。
- **CSI 口径**：数据集=Chickenpox、PedalMe、WikiMaths、METR-LA、PEMS-BAY，全部是图节点时间序列，没有雷达数据（Tab.1 p3）；帧=Chickenpox 4→1，PedalMe 4→1，WikiMaths 8→1，METR-LA 12→12，PEMS-BAY 12→12（Tab.1 p3）；分辨率=图节点，节点数 15–1068，不是网格（Tab.1 p3）；刻度=不适用。Tab.5 表注写明 raw 是 z-score 后的水平值、loader 是 z-score 后的一阶差分；Tab.3 的刻度没写；交通数据是速度，单位没写；阈值=不适用：没有 CSI/HSS。小数据集报 test MSE（Tab.2 p7、Tab.3 p8、Tab.5 p15）；交通数据报第 12 步（60 分钟）的 MAE/RMSE/MAPE（Tab.4 p14）；聚合=不适用（无 CSI）；出处=Tab.1 p3；Tab.2 p7；Tab.3 p8；Tab.4 p14；Tab.5 p15；附录 A p13–p14

| 零件 | 去壳算子 | 插到我方 | 对准问题 | 越协议 | 已关闭族 | 单独消融证据 |
|---|---|---|---|---|---|---|
| 强基线预测作为第二模型的条件协变量（stacking 式融合） | 先冻结一个强的简单基线 f0（原文用 SARIMA），第二个模型的输入是原序列拼上 f0 对目标时刻的预测 ŷ_{t+1,pred}，再由第二个模型输出最终预测（p8 Setup）。放到我方：把 SimVP 的确定性预测当作额外的条件通道，喂给流匹配生成器或一个小修正网络，替代现在的逐像素均值/max 融合 | 融合 / 条件 | 融合 | 否（基线和第二模型都只用同一个训练集；我方照搬时 SimVP 也只看 5 帧） | 否（这里只把基线输出当条件用；原文配套的残差目标属于已关闭族，见零件2） | 没有单独消融：原文把三件东西一起加入（ŷ_{t+1,pred} 协变量、过去 SARIMA 误差通道、以 SARIMA 误差为训练目标，p8 Setup），没有拆开。组合效果，相对 SARIMA 单独（Tab.3 p8，MSE）：Chickenpox 0.827→0.779，PedalMe 1.676→1.632，WikiMaths 0.507→0.446。Tab.3 里的 GCRN-GRU 1.128/1.622/0.657 是表注所说'抄自文献'的上半块，不是同协议重跑的结果；作者自己在 Tab.5 p15 重跑的 GConvGRU（同引 Seo et al. 2018）loader 模式 test MSE 为 0.799±0.004。所以在 Chickenpox 上，和同类 GNN 单独相比只有 0.799→0.779（两张表协议接近，但论文没有声明完全一致）。交通数据（Tab.4 p14）：METR-LA MAE 从 SARIMA 的 5.28 降到 SARIMA_GCRN-GRU 的 3.32，STAEformer 是 3.34；但 p14 写明交通 SARIMA 用固定阶 (4,1,4)×(4,1,4,12)、'unfair' 不限阶协议，按 p13 公式算最大滞后是 65 步，超过 12 步输入窗口，所以这个 SOTA 对比的输入窗口不对等。PEMS-BAY MAPE 4.28 也不如 STEP 的 4.18。我方推断（原文没有讨论）：我方 SimVP 记忆化严重，如果直接用它在训练集上的样本内预测当条件，训练和测试的分布会错配，应该改用 K 折 out-of-fold 预测 |
| 基线残差作为训练目标（学 y − f0(x)） | 第二模型改为预测基线误差，最终输出 f0 加上这个残差 | 训练损失 / 结构 | 融合 | 否 | 是：已关闭族：确定/残差分解 | 无单独消融（与零件1 同在 Tab.3 p8、Tab.4 p14） |
| 基线在历史窗口内的过去误差作为输入通道 | 对历史窗口里的每个时刻 t−τ（τ=1..H），把基线误差 ŷ_{t−τ,pred} − y_{t−τ,true} 作为额外输入通道（p8）。放到我方 5 帧输入：可以用持续性预报误差（也就是相邻帧差），或者一个短上下文基线对第 2–5 帧的'预报误差'，作为条件通道 | 条件 | 高阈值欠报 / 其它:增强与新生事件的信号提示（我方推断，原文没有证据） | 否（误差只由那 5 帧输入算出） | 否 | 没有单独消融（和零件1、2 一起加入，Tab.3 p8 / Tab.4 p14） |
| 不做差分：预测绝对水平而不是增量 | 目标用原始水平 y_t，不用 Δy_t = y_t − y_{t−1}。原文论证一阶差分是高通滤波，f_z(ω)=4sin²(ω/2)f_y(ω)，会放大高频噪声、削弱低频信号，导致过拟合（p5）。对我方是一个自查项：确认 SDIR/FlowCast/SimVP 有没有把'相对最后一帧的增量'当成预测目标 | 训练损失 / 结构（输出参数化） | 记忆化 | 否 | 否（改的是目标参数化，不是频率分解；只是论证借用了谱的语言） | Tab.5 p15（Chickenpox，10 个种子）。DCRNN：原始水平训练 gap/tr=+0.086，差分训练 +0.218；原始水平训练的模型把预测差分后 testDS MSE 为 0.759，差分训练直接得到 0.792。GC-LSTM：gap/tr +0.011 vs +0.347；testDS 0.767 vs 0.784。12 个模型的 raw→testDS 都优于 loader 的 test MSE，但差距比并非全都缩小（TDE-GNN raw −0.075 vs loader −0.059）。表注说明 MSE 跨模式不可比，能比的只有 testDS 和 loader test 这一对。这是图节点 MSE 口径，不是 CSI |
| 泛化差距比 gap/tr = (test−train)/train 加多种子报告 | 每个模型都报告训练误差、测试误差和两者的相对差距，用 10 个种子报均值±标准差，作为过拟合诊断指标 | 读出（评测/诊断） | 记忆化 | 否 | 否 | 只是诊断指标，不是涨分零件（Tab.5 p15） |
| 滞后时间、空间、偏空间相关诊断 | 逐节点（放到我方可以逐像素）计算三种量：自相关 ρ_temp(τ)；邻域加权信号的滞后相关 ρ_sp(τ)；先对自身滞后值做最小二乘投影并去掉，再算偏相关 ρ_psp(τ)（p3 式 1–4）。可以用来比较训练集和测试集的时空依赖结构，诊断漂移；也可以检查 5 帧窗口里远端的滞后帧是否还有增量信息 | 读出（分析工具，不进模型） | 测试漂移 / 其它:诊断 | 否 | 否 | 只有图（Fig.1 p4、Fig.2 p5、Fig.3 p6），没有数值表，不涨分 |
| 位置相关参数，应对节点异质性 | 合成实验表明，各节点时间系数 a_i 的方差越大，共享权重的 GNN 相对逐节点 ARIMA 越吃亏。放到我方（我方推断，原文没有提出这个零件）：给每个像素加可学习的偏置/增益图或位置嵌入（STID 式空间身份），用来刻画地形、波束遮挡等固定的空间异质性 | 结构 | 其它:空间异质性（训练集只有 1381 个事件，逐像素参数可能加重记忆化） | 否 | 否 | 只有合成回归：Var({a_i}) 系数 0.0182（SE 0.0004，p<0.001），R²=0.591（Tab.6 p16）；没有真实数据上的消融 |
| 时间傅里叶特征（K=6）编码季节性 | 把时间戳的正弦/余弦傅里叶特征作为额外协变量输入 | 条件 | 测试漂移 | 是：引入了 5 帧雷达以外的时间戳输入（CIKM2017 本身没有时间戳）；原文还是在不限阶（unfair）协议下使用的（p14） | 否 | Chickenpox 上 SARIMA_LSTM 0.797→SARIMA_LSTM-time 0.713，SARIMA_GCRN-GRU 0.779→SARIMA_GCRN-GRU-time 0.722（MSE，Tab.3 p8）；p14 写明 Chickenpox 的 K=6 傅里叶特征属于 unrestricted regime |
| 公平强基线：输入窗口对齐，自动选阶 | 基线的最大滞后被限制为不超过主模型的输入窗口 H，即 s(P+D)+p+d ≤ H（p13）。在小数据集的 restricted regime 下，用 StatsForecast AutoARIMA 在允许的阶里做网格搜索，以 AIC 选阶（p14）；交通数据不适用，用的是固定阶加不限阶。对我方的启示：所有对比方法严格用同样的 5 帧输入，并补一个认真调过的简单基线 | 读出（评测协议） | 其它:评测公平性 | 否 | 否 | WikiMaths 上 RidgeVAR 0.4904，TDE-GNN 0.565（Tab.2 p7）；SARIMA 0.507（Tab.3 p8），但 Tab.3 没有标明这一行是受限阶还是不受限阶。这些都是基线对比，不是给我方涨分的零件 |

- **撞车**：无（融合线（只在概念上相邻））— 领域完全不同：图节点回归，MSE/MAE 口径，没有雷达、没有 CSI、没有生成模型（全文 grep radar/SEVIR/precip/CSI/nowcast/SDIR 零命中）。唯一概念上的交集是'强简单基线 + 深度模型学修正'这种两模型组合，和我方跨范式融合线相邻；但原文做的是残差 stacking，不是生成模型与确定性模型的像素级融合，不构成撞车。强度阈值阶梯、UOT 质量损失、条件流匹配、SDIR 载体这几条线都无关。没有引用 SDIR。
- **值得上机**：1/3 — 能对上我方问题的零件只有一个：'把 SimVP 预测作为生成模型的条件协变量（stacking 融合）'，对准融合线，也在协议内。但证据很间接：只有图节点 MSE 结果；协变量、过去误差通道、残差目标三者绑在一起没拆开消融，而残差目标本身属于已关闭族。组合效果也被夸大了：Tab.3 的 GNN 单独基线是抄自文献的数字，作者自己重跑的同类 GConvGRU 在 Chickenpox 上只从 0.799 到 0.779；交通 SOTA 用的是 65 步滞后的不限阶 SARIMA，输入窗口不对等。另外我方记忆化严重，必须用 out-of-fold 预测当条件，实现成本中等。其余零件要么是诊断，要么越协议，不直接涨 CSI。
- **是否引用/比较 SDIR**：否
- **核查记录（13 条，节选前 3 条）**：核对范围：key_numbers 里的数字全部在原文找到了，表号和页码也都对。Tab.2 p7、Tab.3 p8、Tab.4 p14、Tab.5 p15、Tab.6 p16 都逐行对过；Tab.3 和 Tab.4 还渲染成图片，按排版核了一遍。没有找到出处不明、需要删除的数字。 ／ 解读有大偏差（零件1）：原记录拿'GCRN-GRU 1.128→SARIMA_GCRN-GRU 0.779（Chickenpox）'当组合效果。但 Tab.3 表注写明，双线以上那一块基线是从 Rozemberczki/Errica/Eliasof 的文献里抄来的，不是同协议重跑。作者自己在 Tab.5 p15 重跑的 GConvGRU（同样引 Seo et al. 2018，基本就是 GCRN-GRU），在 loader 模式下 test MSE 是 0.799±0.004。所以在同一篇论文里、协议接近的条件下，这个增益只有 0.799→0.779，不是 1.128→0.779。旁证：同一个 ／ 漏判（零件1、交通 SOTA）：p14 写明，交通数据上的 SARIMA 用固定阶 (4,1,4)×(4,1,4,12)，而且作者自己称之为 'unfair' 的不限阶协议。按 p13 的公式 s(P+D)+p+d 算，最大滞后是 12×5+5=65 步，远超 12 步输入窗口。这意味着 SARIMA_GCRN-GRU 通过 SARIMA 预测间接看到了更长的历史，和 STAEformer 等基线相比输入窗口不对等。换成我方协议，这相当于'多给输入帧'这一类混杂。原记录只在傅里叶特征那个零件提了 unfair 协议，零件1没提。
