# 附录 A（F_视频时空预测）：精读论文逐篇零件表（8 篇）

说明：每篇先由一个代理通读 arXiv 全文 PDF（从 arXiv 官方 GCS 批量桶取得）拆零件，再由另一个代理回到原文逐个核对数字、表号、口径、越协议与已关闭族判断，下面是**核查后的修正版**。“消融证据”列中的数字均来自 PDF 原文表格/正文（写明表号与页码）；标“图读数”的是从图上读的近似值，不能当表格数字引用。评分 0–3 为“值得上机”。任务标签：A1 融合占位 / A2 融合零件 / A3 叙事 / B 会议 / C 小数据泛化 / D 数据集数字 / E 雷达外推方法 / F 视频·时空预测 / COLL 撞车核查。每篇只出现在其主任务文件里（主任务按 A1>COLL>A2>A3>E>D>C>F>B>G 取第一个）。

## 索引

| # | arXiv | 标题 | 全部任务 | 评分 | 撞车 | 代码 |
|---|---|---|---|---|---|---|
| 1 | [2509.24531](#p250924531) | Diffusion Bridge or Flow Matching? A Unifying Framework and Comparative Analysis | F | 1 | 无 | 有代码（已 git clone 确认 |
| 2 | [2510.01184](#p251001184) | Temporal Score Rescaling for Temperature Sampling in Diffusion and Flow Models | F | 1 | 无 | 有代码(已打开github确认含实现 |
| 3 | [2602.01941](#p260201941) | FluxNet: Learning Capacity-Constrained Local Transport Operators for Conservative and Boun | F | 1 | 无 | 有代码。已 git clone 确认 |
| 4 | [2604.21097](#p260421097) | Learning to Emulate Chaos: Adversarial Optimal Transport Regularization | F | 1 | 部分 | 有代码（已 git clone 确认 |
| 5 | [2605.24041](#p260524041) | Iterative Refinement Neural Operators are Learned Fixed-Point Solvers: A Principled Approa | F,G | 1 | 部分 | 有代码（已 git clone 核实 |
| 6 | [2609.04995](#p260904995) | Beyond Homoscedasticity: Decoupled Uncertainty Optimization for Deep Imbalanced Regression | F | 1 | 无 | 无 |
| 7 | [2609.05506](#p260905506) | ECHO: Dyadic 3D Facial Motion Generation with Asymmetric Deterministic Articulation and St | F | 1 | 无 | 无 |
| 8 | [2606.02453](#p260602453) | Initialization is Half the Battle: Generating Diverse Images from a Guidance Potential Pos | F | 0 | 无 | 有代码（已 git clone 核实 |


---
<a id="p250924531"></a>
### Diffusion Bridge or Flow Matching? A Unifying Framework and Comparative Analysis

- **ID**：arXiv 2509.24531　**Venue**：ICML 2026（PMLR 306）
- **Venue 依据**：PDF 第1页页脚："Proceedings of the 43 rd International Conference on Machine Learning, Seoul, South Korea. PMLR 306, 2026."；arXiv v2 [cs.CV] 7 Jun 2026；GitHub README 标题也写 [ICML 2026]
- **代码**：有代码（已 git clone 确认含实现）。FM 部分：flow_matching_transformer/{flow_matching.py, model_x0_x1.py（SiT-L/2，in_channels=4，不接收条件）, train_vae.py, infer_vae.py, calculate_image_metrics.py}。DB 部分：diffusion_bridge_transformer/diffusion bridge/models/modules/DenoisingDiT_arch.py（412行；第320行 x = xt - cond，第323行 x = torch.cat([x, cond], dim=1)），另有 train/test/sde_utils。评估脚本只算 PSNR（RGB，data_range=255）、SSIM（转灰度）、LPIPS(alex)；FID 用外部 pytorch-fid；翻译任务的 MSE/IS 代码未提供；Tab.4 的'FM w/ zT'变体代码未公开。不涉及 CSI。（https://github.com/zhukaizhen/diffusion_bridge_flow_matching）
- **阅读层级**：全文PDF　**核查结论**：corrected　**任务**：F
- **一句话**：通用图像复原/翻译论文，不是雷达论文。作者用随机最优控制证明：扩散桥（UniDB-GOU，γ→∞，即 GOUB 类）的均值回复漂移 θt(x1−xt) 使控制代价不高于流匹配；这里的流匹配从条件图出发、不加噪声、网络也不看条件。作者又从 OT 角度论证 FM 的 t/1−t 线性插值在小训练集下失效。两者在同一个 latent DiT（约4.85亿参数，用预训练 SD VAE）上对比，结果如下。FID：DB 全面领先。LPIPS：复原任务上 DB 领先，Edges→Handbags/Shoes 上 DB 落后。像素指标互有胜负：Box 掩码和 SR 上 PSNR 差很小，SSIM 是 FM 更高；Deblur 上 FM 的 PSNR 更高；不规则掩码在小数据下 DB 的 PSNR/SSIM 大幅领先。核心零件属已关闭族'换源分布'。
- **CSI 口径**：数据集=CelebA-HQ 256（inpainting：Box50–128 与 thin/thick/ev2li 掩码；4×SR；61×61 高斯去模糊；风格迁移）、FFHQ 256（inpainting）、CelebAMask-HQ（翻译）、Edges→Handbags、Edges→Shoes、Nights→Days。训练集规模见 Tab.5 p19（CelebA-HQ inpainting 取 500/1000/5000/27000，FFHQ 69000，其余 10000–27000）。无雷达数据。latent 由预训练 Stable Diffusion VAE 编码（p8），照搬属'预训练'越协议。；帧=不适用（单图→单图）；分辨率=256×256，经预训练 SD VAE 编码为 h/8×w/8×4 的 latent（p8）；刻度=MSE 在 [-1,1] 刻度（p7 原文 'MSE, lower is better, in [−1, 1] scale'）；代码中 PSNR/SSIM 按 0–255 uint8 计算（data_range=255），SSIM 先转灰度；阈值=无 CSI/HSS。指标为 PSNR、SSIM、LPIPS、FID；翻译任务另报 MSE、IS（p7）；聚合=不适用（无 CSI）。代码中 PSNR/SSIM/LPIPS 逐图计算后取均值（calculate_image_metrics.py，np.mean）；出处=PDF p7 §5 指标说明、p8 §5.1（预训练 SD VAE）、Tab.5 p19；代码 flow_matching_transformer/calculate_image_metrics.py

| 零件 | 去壳算子 | 插到我方 | 对准问题 | 越协议 | 已关闭族 | 单独消融证据 |
|---|---|---|---|---|---|---|
| 潜空间扩散桥（UniDB-GOU，γ→∞，即 GOUB 类）替代流匹配 | 前向 SDE 为 dx=[θt(x1−xt)+gt·ut]dt+gt·dw，终点固定在条件 latent。训练时用闭式 zt=ξt·z0+(1−ξt)·zT+σ'ε 采样中间状态，网络输出 ε，但损失是均值匹配 ／／µ_{t−1,θ}−µ_{t−1}／／（Alg.1 p18）。推理从 Encoder(条件) 出发，用一阶 UniDB++ 带噪采样（App.B p17、Alg.2 p18） | 采样 / 训练损失（整条生成范式替换） | 记忆化（论文只主张小训练集下更稳，未测训练/测试差距）；其它：感知质量 | 否（零件本身协议内；论文实现依赖预训练 SD VAE，照搬 latent 管线则越协议） | 是：换源分布（源 = 条件帧 latent） | 只有 DB 与 FM 的整体对比，混杂了源分布、加噪、网络是否看条件等多个因素。Tab.1 p6（Box128 inpainting）PSNR/SSIM/FID：FM 23.54/0.760/17.84，DB 23.57/0.741/7.71。Deblur：FM 24.67/0.683/38.18，DB 24.13/0.661/19.03。4×SR：FM 27.11/0.789/11.61，DB 27.47/0.762/8.50。Box 掩码和 SR 上像素指标差距很小，Deblur 上 FM 的 PSNR 更高。但不规则掩码上 DB 的像素指标明显领先，见 Tab.7 p20：n=500 thin 掩码 PSNR/SSIM FM 18.73/0.588 vs DB 23.41/0.690；满数据下 Tab.8 p21 ev2li 为 25.69/0.731 vs 26.99/0.749 |
| 均值回复漂移项 θt(x1−xt)，并取扩散系数 gt=1（与 FM 控制项形式一致的变体） | 在 FM 的平凡 ODE dx=u·dt 上加一个指向条件端点的线性回复漂移，理论上可降低总控制代价 J（Thm 4.2 p5，假设 gt=1）。D.5 实测了 gt=1 的 DB，噪声项仍然保留 | 训练损失 / 采样（路径定义） | 其它：大分布差异下的稳定性 | 否 | 是：换源分布（漂移指向条件端点，前提是源 = 条件） | Tab.12 p22（LPIPS/FID）：DB(gt=1) Box96 0.052/5.96，Box128 0.079/7.86；FM Box96 0.060/8.18，Box128 0.106/17.84；原 gt 的 DB Box96 0.052/6.25，Box128 0.078/7.71。只有感知指标，没有像素指标 |
| 小训练集下 DB 相对 FM 的鲁棒性（训练集规模扫描） | 同一结构下训练集取 500/1000/5000/27000，比较两种范式的退化速度 | 训练（范式选择） | 记忆化（映射偏松：论文没有测训练/测试差距） | 否 | 是：换源分布（结论依赖源 = 条件的 FM/DB，未测高斯源条件 FM） | Tab.7 p20（n=500/1000/5000）和 Tab.8 p21（标题未写规模，由 Tab.5 与 Tab.1/2 数值推断为 n=27000）。Box128 PSNR/SSIM/FID 如下。n=500：FM 21.43/0.719/45.81，DB 21.70/0.705/11.34。n=1000：FM 21.76/0.724/37.23，DB 22.23/0.717/9.43。n=5000：FM 22.65/0.744/17.87，DB 22.96/0.733/8.59。n=27000：FM 23.54/0.760/17.84，DB 23.57/0.741/7.71。Box128 上 PSNR 差只有 +0.03~+0.47 dB，SSIM 是 FM 更高。不规则掩码在小数据下 DB 大幅领先，PSNR/SSIM 如下。n=500：thin FM 18.73/0.588 vs DB 23.41/0.690；thick 17.95/0.678 vs 21.10/0.700；ev2li 24.27/0.681 vs 25.42/0.702。n=1000：thin 20.00/0.625 vs 24.13/0.711 |
| 残差式条件输入：concat[x_t − cond, cond] | 网络输入先算 x_t 减去条件，再在通道维与条件拼接（p19 原文；代码 DenoisingDiT_arch.py 第320、323行）。迁移到我方：可以把 cond 取为最后一帧或持续性外推的 latent，让网络直接看到 x_t 相对持续性的残差 | 条件 / 结构 | 其它：条件利用效率（间接对准增强/新生导致的高阈值欠报） | 否 | 否（如果把 cond 设计成'持续性 + 残差'并当成卖点，就接近已关闭的'确定/残差分解'表述，只能当工程细节用） | 减法这一步没有单独消融。Tab.4 p8 只消融了'给源=条件的 FM 额外输入 zT'，LPIPS/FID 变化如下。Box128：0.106/17.84→0.093/14.66。Box96：0.060/8.18→0.058/7.11。Box64：0.039/5.13→0.039/5.20（FID 变差）。我方条件 FM 本来就输入条件，这组消融与我方无关 |
| 非线性插值系数 ξt = e^{−θ̄t}·σ̄²_{t:T}/σ̄²_T，配翻转余弦 θt 调度（替代 t/1−t） | 把线性插值换成由累计漂移决定的指数型插值系数（Alg.1 p18），θt 用 s=0.008 的 flipped cosine 调度（式44 p19）；论文里这个插值还带桥噪声 σ̄'_t·ε | 训练损失（路径 / 时间调度） | 其它：路径曲率 / 小数据 | 否 | 否（保持高斯源、只换插值调度不在关闭列表里；但论文中它与换源、漂移绑在一起） | 无单独消融 |
| ODE 确定性采样 vs SDE 带噪采样对像素指标的取舍 | FM 用 Euler ODE（零附加噪声），DB 用 SDE 注入噪声。论文 p8 的结论是 ODE 的像素失真指标更好，SDE 的感知指标更好 | 采样 / 读出 | 其它：像素指标（CSI 属于像素阈值类指标）与感知指标的取舍 | 否 | 否 | 没有干净的单独消融（与范式绑定）。支持论文结论的数据：Tab.1 p6 inpainting/deblur/SR 三项的 SSIM 都是 FM 更高（0.760/0.683/0.789 vs 0.741/0.661/0.762）；Edges→Shoes 的 MSE FM 0.034 vs DB 0.100。反例：Edges→Handbags 的 MSE DB 0.102 < FM 0.130；不规则掩码上 DB 的 SSIM 更高（Tab.7 p20 n=500 thin 0.690 vs 0.588；Tab.8 p21 ev2li 0.749 vs 0.731） |
| NFE 扫描 | 采样步数取 10/20/50/100 | 采样 | 其它：采样步数 | 否 | 否 | Tab.10 p21，Box128 的 LPIPS/FID（NFE 依次为 10/20/50/100）。FM：0.111/23.01、0.106/17.84、0.099/17.40、0.101/17.51。DB：0.078/9.32、0.078/7.71、0.079/7.20、0.080/7.14。20 步以后基本饱和 |

- **撞车**：无（条件流匹配临近预报线（融合线 / 强度阈值阶梯线 / UOT 质量损失线 / SDIR 载体线：均无））— 这篇不做雷达，也不做融合、强度阶梯或 UOT，不构成撞车，但有审稿质疑风险。它的主张是'FM 的 t/1−t 线性插值在小训练集下失效，扩散桥更稳'；它的小数据区间是 500–27000 张，我方 1381 个事件正好落在里面，审稿人可能引它质疑'为什么用 FM 而不用桥'。主要反驳：(1) 它的 FM 是'源 = LQ 图'、不加噪、网络不看条件的 FM（Alg.3/4 p18；代码 model(x=x_t,t=t)，in_channels=4），不是我方的高斯源条件 FM；给它的 FM 加上条件输入后差距就缩小（Tab.4 p8，Box128 FID 17.84→14.66）。(2) 没有雷达或 CSI 实验。需要承认的一点：像素指标并不总是 FM 更好。Box 掩码和 SR 上差距小（PSNR ≤0.47 dB，SSIM FM 更高），但不规则掩码在小数据下 DB 的 PSNR 领先 3–4.7 dB（Tab.7 p20，n=500 thin 18.73 vs 23.41），所以不能用'像素指标持平'来反驳。旁证（弱）：Tab.11 p22 中 150M 参数的 U-Net 桥（GOUB 28.63、UniDB 28.70）PSNR 高于 485M 参数的 latent Transformer（FM 27.11、DB 27.47）；但 SSIM 持平或更低，LPIPS/FID 更差，而且混杂了结构、latent/像素（'像素空间'是按原论文推断的，本文没写）和训练设置。它只能弱支持'latent 模型像素保真偏低，所以与像素级确定性模型融合'这一动机。
- **值得上机**：1/3 — 主干零件（扩散桥、回复漂移、小数据鲁棒性）都属于已关闭族'换源分布'。论文实现依赖预训练 SD VAE，照搬越协议。协议内、实现便宜的只有两个：残差式条件输入 concat[x_t−cond, cond] 和非线性插值调度，都没有单独消融。论文没有 CSI 或雷达实验。主要价值在于三点：作为相关工作引用；提前准备'小数据下为何用 FM'的防守（注意不规则掩码上 DB 的像素指标也明显更好）；作为'latent 模型像素保真偏低'的弱旁证。
- **是否引用/比较 SDIR**：不适用（非雷达论文；全文检索无 SDIR/radar/precipitation 相关引用）
- **核查记录（12 条，节选前 3 条）**：数字核对：key_numbers 和 ablation_evidence 里的每个数字都对照了 PDF 抽取文本，并渲染 p6、p8、p20 核对了表格排版（Tab.1 p6、Tab.2 p7、Tab.3/Tab.4/Fig.4 p8、Tab.5/Tab.6 p19、Tab.7 p20、Tab.8/Tab.9/Tab.10 p21、Tab.11/Tab.12 p22）。表号、页码、数值全部准确。Fig.4 渲染图确认：FM 训练 20h、推理 0.346s；DB 训练 66h、推理 0.362s。 ／ 过度概括（重要）：parts[0] 写'像素指标上 DB 基本没有优势'，parts[2] 写'PSNR 差仅 +0.03~+0.47 dB，SSIM 则 FM 更高'，collision 反驳点(2) 写'PSNR 差距 ≤0.47 dB'。这些只在 Box128（以及 Tab.1 的 SR）上成立。Tab.7 p20 的不规则掩码在小数据下，DB 的像素指标大幅领先。n=500 时 PSNR/SSIM：thin 掩码 FM 18.73/0.588 vs DB 23.41/0.690（+4.68 dB）；thick 17.95/0.678 vs 21.10/0.700；ev2li 24.27 ／ one_liner 说'DB 的 LPIPS/FID 大幅领先'不准确。Tab.1 p6：Edges→Handbags 的 LPIPS FM 0.189 < DB 0.232，Edges→Shoes FM 0.176 < DB 0.228，IS FM 3.57 > DB 2.97。论文 p8 说'DB consistently ... superior perceptual scores (LPIPS and FID) across all tasks'，与它自己的表不符；只有 FID 是 DB 全部领先。MSE 也不是全部 FM 更好：Edges→Handbags 上 DB 0.102 < 

---
<a id="p251001184"></a>
### Temporal Score Rescaling for Temperature Sampling in Diffusion and Flow Models

- **ID**：arXiv 2510.01184　**Venue**：ICML 2026 (PMLR 306)
- **Venue 依据**：PDF p.1 脚注："Proceedings of the 43 rd International Conference on Machine Learning, Seoul, South Korea. PMLR 306, 2026."；arXiv v2（22 May 2026）。GitHub README 亦标 ICML 2026；代码文件 TSR_schedulers.py 头注释仍写 "Supplementary material for ICLR 2026 submission"，属旧投稿残留，不改变场地判断。
- **代码**：有代码(已打开github确认含实现)：git clone 核实（HEAD a09d95b, 2026-06-12）。TSR_diffusers/TSR_schedulers.py 实现 TSR_DDIMScheduler（仅支持 ε 预测，ratio=(ᾱσ²+1−ᾱ)/(ᾱσ²/k+1−ᾱ)）与 TSR_FlowEulerScheduler（ratio=(snr·σ²+1)/(snr·σ²/k+1)，snr=(1−t)²/t²；速度改写 (ratio((1−t)v+x)−x)/(1−t)）；TSR_toy/sampler/toy_samplers.py 含 DDPM/flow-ODE/flow-SDE 的 TSR 与 CNS 步；另有 t2img_demo.ipynb 与玩具训练/采样脚本。仅覆盖 SD2/SD3 文生图与 2D 玩具；深度/位姿/机器人/蛋白实验无代码。非雷达论文，无 CSI 评估代码。（https://github.com/temporalscorerescaling/TSR）
- **阅读层级**：全文PDF（26页含附录与证明；核查时渲染 p.7 核对 Tab.1/Tab.2 排版）　**核查结论**：corrected　**任务**：F
- **一句话**：通用、免训练、零额外 NFE 的采样旋钮：每步把预测 score/ε̂ 乘时间相关因子 r_t(k,σ)=(η_tσ²+1)/(η_tσ²/k+1)（η_t 为 SNR；高噪声时≈1，t→0 时→k）；流模型改写为 ṽ=α⁻¹(r(αv−α̇x)+α̇x)。效果是把每个模态附近的局部方差缩放 1/k 而不改模态权重，兼容确定性 ODE 采样。k>1 锐化（深度/位姿/机器人提升），k<1 展平（图像细节增多、FID 降）。非临近预报论文，无 CSI。
- **CSI 口径**：数据集=非雷达。文生图 SD3/SD2/Flux.1-dev（FID/CLIP，对 LAION Aesthetics 5k 子集，30 步采样）；Marigold 深度（ETH3D、NYUv2）；SO(3) 位姿（SYMSOL）；Pi-0 机器人（LIBERO-10）；FoldingDiff 蛋白；MNIST 模态塌缩检查；2D 玩具分布；帧=不适用；分辨率=不适用；刻度=不适用；阈值=不适用（无 CSI/HSS；指标为 FID、CLIP、AbsRel、δ1、旋转误差、成功率、designability）；聚合=不适用；出处=全文PDF：Sec.3–5、Tab.1–5（p.7/8/9/14）、App. A–C

| 零件 | 去壳算子 | 插到我方 | 对准问题 | 越协议 | 已关闭族 | 单独消融证据 |
|---|---|---|---|---|---|---|
| TSR 时间相关 score/ε 重标定（核心算子） | 每个 ODE/SDE 步把 s_θ 换成 r_t(k,σ)·s_θ，r_t=(η_tσ²+1)/(η_tσ²/k+1)，η_t=α_t²/σ_t²（Eq.6）；ε 预测直接用 r_t·ε̂（Eq.7）；流速度用 Eq.9 ṽ=α_t⁻¹(r_t(α_t v−α̇_t x)+α̇_t x)，α=1−t 时即 (r((1−t)v+x)−x)/(1−t)，等价于把隐含 ε̂ 乘 r 后重算速度。免训练、零额外 NFE（论文 p.2 称 zero inference overhead）；代码改动约十行（核查者估计，非论文数字）。 | 采样（FlowCast latent 条件流匹配的 ODE 采样器；需先把我方 α_t/σ_t 约定映射到论文的 x_t=α_t x0+σ_t ε，作用在 latent 空间） | 高阈值欠报（推测、间接）；其它:采样温度。方向未知，k 应在 1 两侧都扫：论文中图像保真任务受益于 k<1（Fig.4/Fig.11 细节/高频增多），而对单一真值打分的预测类任务（深度/位姿/机器人）受益于 k>1；雷达 CSI 属后者但高阈值欠报可能需 k<1 的增细节效应。融合（推测：作为生成成员与 SimVP 融合前的旋钮）。关键保留：我方 8 采样余弦 0.998，学到的条件分布近乎点质量，按理论 TSR 只把模态附近标准差缩放 1/√k，预期效应小；但实际 σ 为自由超参、学到的 score 非理想，效应需实测而非假定。不针对记忆化与测试漂移。 | 否（免训练采样改动；(k,σ) 须在从训练集切出的验证集上调，不能在测试集上调） | 否（边界）：score 形式下 TSR=r_t·s_θ，形式上等价于无条件 score 取零、权重随时间变化的 CFG，故与已关闭族"CFG 及各类引导"相邻；但不需无条件分支/条件 dropout、不换源分布，目标是局部温度，严格意义上不在已关闭清单内。若组内把任何采样期 score/速度缩放都算引导，则按已关闭处理。 | 无雷达证据。其它领域 TSR 单因子 vs 默认采样器：Tab.1 p.7 SD3 FID 24.77→22.81、CLIP 32.82→33.05；SD2 22.81→19.75 / 33.66→33.75；Flux.1-dev 53.99→51.79 / 31.97→32.14（SD2/Flux 用 SD3 调得的参数迁移，p.6）。Tab.2 p.7 Marigold ETH3D AbsRel 7.1→6.68，NYUv2 AbsRel 6.0→5.84（DDIM 行 ETH3D δ1=90.4 与 CNS/TSR 95.6/95.7 差距异常，疑论文笔误，勿引 δ1 增益）。Tab.3 p.8 位姿误差 0.444→0.356。Tab.4 p.9 Pi-0 LIBERO-10 平均成功率 81.7→82.8（6 任务升、2 持平、任务2 80.7→77.3 与任务8 23.3→21.3 降；表注写明用 best (k,σ)，3 seeds×50 rollouts）。Tab.5 p.14 SD2 DDIM FID 21.28→20.05、EulerDiscrete 22.11→19.95。p.6 正文 SD3 5 种子：FID 24.77(±0.10)→22.81(±0.13)。注意：SD3 最优参数按同一 5k 评测子集的 FID 选出，论文无独立调参划分，增益偏乐观。 |
| (k,σ) 双旋钮：k 定终点缩放、σ 定开始干预的时刻 | k 为 r_t 的极限（t→0 时 r_t→k），决定局部方差缩/放程度；σ 决定多早开始重标定：σ 小时大部分轨迹 r_t≈1，只干预最后几步去噪（Sec.3.2 p.4, Fig.3 右）。实用上即"只改末段"vs"改整条轨迹"。 | 采样 | 高阈值欠报（推测：只改末段可调局部纹理/强度而不动大尺度平流结构） | 否 | 否（同第1项的边界说明） | 仅图无数表：Fig.9 p.12（SD3 (k,σ) 扫描，最优 k=0.93, σ=3.0）、Fig.12 p.14（位姿对 σ 不敏感，最优 k≈7，图注原文 'TSR is not sensitive to σ in pose estimation, TSR reaches optimal performance with k ≈7'）、Fig.14 p.16（深度）。 |
| Constant Noise Scaling (CNS)（对照基线） | 把反向 SDE 噪声项乘 1/√k、漂移不变：dx=[f−g²∇log p]dt+(g/√k)dw̄（Eq.1）。只能用于随机采样器；App. C（p.25–26）证明除非先验恰为 q_T 否则无法得到精确温度缩放；玩具/MNIST 上偏向中心模态、丢模态。 | 采样（需把我方流模型从 ODE 采样改为 SDE 采样） | 其它:采样多样性控制（不直接对应我方问题） | 否 | 否 | Tab.3 p.8 位姿：CNS(1600) 误差 0.350 略优于 TSR(7.0,0.5) 0.356（基线 0.444）。Tab.2 p.7 ETH3D AbsRel CNS 6.82 vs TSR 6.68。Tab.5 p.14 SD2 DDPM FID 22.81→CNS(0.96) 19.87→TSR(0.9,1.0) 19.57。App. A.1 / Fig.10 p.12：SD3 上 SDE 采样远差于 ODE，作者称 CNS 对流模型 'impractical'。 |
| Constant Score Scaling (CSS)（反例对照） | 每步把 score 乘常数 k：dx=[f−k g²∇log p]dt+g dw̄（Eq.10，App. A.6 p.18），即 TSR 的时间无关版。 | 采样 | 其它:仅作说明为何需要时间相关 r_t 的反例 | 否 | 否 | 仅 2D 玩具定性图（Fig.19 p.19，checkerboard 上 CSS 与 CNS 一样丢模态），无数字。 |
| (k,σ) 坐标式二分搜索与跨模型迁移 | 先固定 σ（如 1.0）二分搜 k*，再固定 k* 二分搜 σ，必要时再来一轮；作者观察到单参数响应近似抛物线，称每模型调一次即可且可跨模型迁移（Sec.5.6 p.9）。 | 采样（采样器超参在验证集上调） | 其它:采样旋钮调参成本 | 否（前提是在从我方训练集切出的验证集上搜；在测试集上搜即泄漏——论文本身即在评测集上选参） | 否 | Tab.1 p.7：SD3 上得到的 (k,σ)=(0.93,3.0) 迁移到 SD2（FID 22.81→19.75）与 Flux.1-dev（53.99→51.79）。抛物线趋势仅见 Fig.9/12/14，无表。 |
| 与 CFG 叠加 | 已关闭族：CFG 及各类引导 | 采样 | 不推荐 | 否 | 是:CFG 及各类引导 | 仅 Fig.5 p.7 / Fig.10 p.12 曲线，无表 |

- **撞车**：无（条件流匹配临近预报线（仅擦边））— 通用采样旋钮，无雷达/临近预报实验；与我方跨范式融合、强度阈值阶梯、UOT 质量损失、SDIR 载体均不重叠。至多可作为 FlowCast 载体上的一条采样消融（"TSR k 双侧扫描"）。它只与已关闭族"CFG 及各类引导"相邻，本身未被关闭。
- **值得上机**：1/3 — 协议内、几乎零成本（免训练、零额外 NFE；代码改动约十行、验证集扫几组 (k,σ) 约一小时为核查者估计），且在多个非雷达领域有干净的单因子证据（Tab.1–5）。但 TSR 只缩放模态附近局部方差，我方 8 采样余弦 0.998、条件分布近点质量，可动空间有限；对高阈值欠报的收益纯属推测（且 k 的有利方向未知，论文中预测类任务偏 k>1、图像保真偏 k<1）；论文参数均在评测集上选出，增益偏乐观；不触及记忆化与测试漂移。值得在验证集上做一次双侧 k 扫描当 sanity check，不作主线。
- **是否引用/比较 SDIR**：不适用
- **核查记录（12 条，节选前 3 条）**：All key_numbers and ablation_evidence figures checked against the PDF text, with p.7 rendered to confirm Tab.1/Tab.2 alignment. Every figure matches: Tab.1 p.7, Tab.2 p.7, Tab.3 p.8, Tab.4 p.9, Tab.5 p.14, the 5-seed error bars and k=0.93/σ=3.0 on p.6, MNIST 40.4%/28.7% on p.16. Nothing had to be de ／ Label format: the enumerated fields (code_status, read_level, collision.level, out_of_protocol, closed_family, the insert_at/targets labels and tasks) were written in English and did not use the label vocabulary the task specifies (有代码…/全文PDF/无/否/采样/高阈值欠报/其它:xxx, etc.). All are now switched to the s ／ Part 5 (coordinate-wise binary search for (k,σ)) had insert_at 'Readout'. It is hyperparameter tuning of the sampler, so it is changed to '采样' (Sampling).

---
<a id="p260201941"></a>
### FluxNet: Learning Capacity-Constrained Local Transport Operators for Conservative and Bounded PDE Surrogates

- **ID**：arXiv 2602.01941　**Venue**：ICML 2026 (PMLR 306)
- **Venue 依据**：p.1 页脚原文："Proceedings of the 43 rd International Conference on Machine Learning, Seoul, South Korea. PMLR 306, 2026."；arXiv v2（2026-05-26，cond-mat.mtrl-sci）
- **代码**：有代码。已 git clone 确认（commit ec0cafe，2026-05-26）：src/models 下有 fluxnet_n/p/l/d 的 1d 与 2d 版、fluxnet_u_1d（没有 u 的 2d 版）、fluxnet_sw_lap/fluxnet_sw_2d、fluxgnn_1d_boundary/fluxgnn_d_1d_boundary、FNO/CNN 基线，模型代码共约 6670 行；另有 experiments/ 多种子训练脚本、dataset/ 数据生成（含 CUDA 相场求解器）。非雷达论文，全仓不计算 CSI。（https://github.com/Lan-zs/FluxNet）
- **阅读层级**：全文PDF　**核查结论**：corrected　**任务**：F
- **一句话**：ICML 2026 的 PDE 代理模型论文，不是雷达论文。网络不直接回归下一帧，而是按方向分通道输出累计搬运量 F_{i→i+d}，用守恒更新 u'=u−ΣF_out+ΣF_in 让全局总量精确守恒。另有容量约束头 sigmoid(外流比例)×softmax(方向分配)×(u−ℓ 或 umax−u)：L 头从结构上保证下界，U 头保证上界；D 头双分支取平均，双边界只在经验上成立。只在 4 个 PDE 基准上用 MAE、守恒误差、越界率评估，没有 CSI。核心的硬守恒不允许新生和增强，和我方高阈值欠报的需求方向相反。
- **CSI 口径**：数据集=1D 对流扩散（32 格点，100/10/10 条轨迹，p.14）；2D 浅水方程（64×64，50/20/50，p.15）；1D LWR 交通流（256 格点，周期边界 100/50/100，p.16–17；Dirichlet 边界 50/50/50，p.17）；2D Cahn–Hilliard 旋节分解（128×128，单轨迹 one-shot 训练，测试 20 条，p.19）。没有任何雷达或降水数据。；帧=单步自回归 u_t→u_{t+1}，推理时 rollout。浅水和交通流训练用 pushforward 展开 P=5 步（p.15、p.17、p.18）；对流扩散是单步训练、不做 pushforward（p.14 §B.2）；旋节分解附录没写。测试外推到 2× 训练时长，对流扩散例外，在 T=1 评估。；分辨率=1D 32/256 点；2D 64×64 与 128×128；刻度=物理量：浓度、水深/动量、归一化密度 ρ∈[0,1]、ϕ∈[0,1]，不是 dBZ；阈值=不适用：没有 CSI/HSS。指标是 EMAE、守恒误差 Econs、越界率 Vlb/Vub（%）、条件越界幅度 Mlb/Mub（p.6）；聚合=不适用（论文和代码都不算 CSI）；出处=PDF §3–4（p.3–9），附录 A–E（p.12–20）；代码 github.com/Lan-zs/FluxNet

| 零件 | 去壳算子 | 插到我方 | 对准问题 | 越协议 | 已关闭族 | 单独消融证据 |
|---|---|---|---|---|---|---|
| 守恒搬运更新（按方向分通道的累计搬运量，移位后求入流） | 网络输出 K 个方向场 F_d，next = u − Σ_d F_d + Σ_d roll(F_d, −d)（Eq.2 p.3；代码 fluxnet_l_2d.py 的 _shift_tensor 用 torch.roll(shifts=(-dh,-dw)) 实现）。全局和精确不变，没有源项。 | 结构（输出头：把直接回归换成局部搬运头） | 其它:质量守恒。对我方高阈值欠报是反方向的：没有源项，不能新生或增强，而我方欠报恰好集中在增强/新生事件。 | 否 | 是:位移/形变（判断：逐像素学习的局部搬运核近似核式 warping，有一定主观性）。若照 p.9 所说用算子分裂补源项，就落入已关闭族“平流/生消分解” | 同骨干、只换头的对比（PDE MAE，不是 CSI）：交通流 ResNet-AR 8.09±0.91 vs FluxNet-D 2.79±0.79（×10^-3，Tab.4 p.8）；浅水 h：ResNet-AR 72.8±84.3 vs FluxNet-LAP 1.75±0.11（×10^-3，Tab.3 p.7） |
| L 头：容量约束下界头（sigmoid 外流比例 × softmax 方向分配 × 可用量 u−ℓ） | F_{i→i+d} = (u_i−ℓ)·σ(α̂_i)·softmax_d(π̂_i,d)。总外流小于 u_i−ℓ，因此 u'>ℓ（Prop.2 p.4） | 结构/读出 | 其它:非负/有界。雷达 dBZ≥0 用 clamp 就能满足，与 CSI 无关 | 否 | 否（但它依附于上面的搬运更新，整体同样落在位移/形变族内） | 对流扩散 EMAE：N 头 16.73±24.22、P 头 1.51±0.45、L 头 1.36±0.41（×10^-3，Tab.2 p.6）。反面证据：对输出做 sigmoid 压缩的 SigmoidBound+SoftCons 为 82.7±18.5，FluxNet-D 为 2.79±0.79（×10^-3，Tab.4 p.8），说明硬挤压输出会大幅损失精度 |
| U 头：容量约束上界头（按剩余容量 umax−u 限制入流） | F_{(i+d)→i} = (umax−u_i)·σ(β̂_i)·softmax_d(ρ̂_i,d)，保证 u'<umax（Prop.3 p.4） | 结构/读出 | 其它:上界。对雷达基本无益：若 umax 取物理上限，影响很小；真正压制增强的是守恒本身 | 否 | 否（同样依附于搬运更新） | 交通流 EMAE：FluxNet-U 2.14±0.54，FluxNet-P 4.40±1.35（×10^-3，Tab.9 p.17）；U 头下界越界率 2.53%（Tab.9 p.17） |
| D 头：同一搬运量的双视角异参数化分支取平均，外加双分支一致性损失 DCL | 出流视角分支（L 型）和入流视角分支（U 型）各算一个 Δu，最终 u' = u + ½(Δu_out + Δu_in)，另加 L_DCL = mean／Δu_out − Δu_in／²（Eq.3–4 p.4）。两分支共享同一骨干，联合训练 | 结构（网络内双头平均）；DCL 属于训练损失 | 融合（极弱、间接）。去掉 DCL 后精度最好，但论文对 DCL 的定位是拿精度换双边界满足，不是融合成员之间的一致性约束；两分支共享骨干、不是跨范式成员。所以只能作为“成员之间不要强行拉近”的极弱旁证，不能当作我方融合结论的支持证据 | 否 | 否 | 交通流 EMAE（×10^-3）：D (w/o DCL) 1.52±0.14 最低；带 DCL 的 FluxNet-D 2.79±0.79；单分支 L 2.68±0.75、U 2.14±0.54（Tab.9 p.17）。代价是越界率从 Vlb 0.59%/Vub 0.01% 升到 2.26%/0.41%（Tab.9 p.17）。注意：单分支是另外单独训练的模型，通道数也不同，不是纯粹的“平均”消融；而且是 PDE MAE，不是 CSI；5 个种子 |
| Pushforward 训练（展开 P 步、截断梯度，1 步损失 + 第 P 步损失） | û_{p+1}=T_θ(sg(û_p))，L = ‖û^1−u^1‖² + ‖û^P−u^P‖² + γL_DCL（Eq.6 p.5） | 训练损失 | 其它:自回归误差累积 | 否 | 是:rollout/self-forcing 式训练 | 浅水 h EMAE：LAP w/o pushforward 2.73±0.32 vs LAP 1.75±0.11（×10^-3，Tab.7 p.16）；交通流 D w/o pushforward 4.08±0.46 vs 2.79±0.79（×10^-3，Tab.9 p.17） |
| 可配置搬运邻域半径 R（大时间步长配大 stencil，不受 CFL 限制） | stencil 半径 R 覆盖单个模型步内的最大位移，2D 方向通道数 K=(2R+1)^2−1；不做时间积分（Remark 1 p.3）。交通流 10Δt 用 R=5，50Δt 用 R=7（p.17–18） | 结构 | 其它:大位移/长步长直出（5→20 帧直出时需要很大的 R） | 否 | 是:位移/形变 | 无单独消融。Tab.6 p.9 中 R、步长和骨干（块数、核大小，见 p.19 §E.3）同时变化，混杂：10Δt R=1 为 2.76，100Δt R=2 为 2.16，1000Δt R=4 为 8.39（EMAE ×10^-2）。Tab.5 p.8 在 50Δt 下 FluxNet-D 50.1±4.85 vs FluxGNN 1150±87.9（×10^-4），属跨方法对比 |
| 幽灵单元 + 内部/幽灵二值标识通道（用于非周期边界） | 在四周填充 R 格幽灵单元（按边界条件赋值，Dirichlet 情形填已知的定常边界值），输入追加 1/0 标识通道，骨干改用 replicate padding，只保留内部结果（§3.6 p.5；§D.3.2 p.18） | 条件/结构 | 其它:边界入流（雷达域外回波流入）。但雷达未来的边界值未知，没法像 Dirichlet 那样直接给定 | 否（若在雷达上只用输入帧填充则协议内；用未来边界真值则是泄漏） | 否 | Dirichlet 交通流 EMAE（×10^-4）：只按周期边界处理的 FluxNet-D 717±20.8，加幽灵单元后 11.7±0.23（Tab.5 p.8）。这只对硬守恒模型有意义，不能迁移到我方的非守恒模型 |
| 局部、平移不变的小感受野搬运算子，支持小数据/one-shot 学习（问题表述） | 学到的是局部搬运规则，有效感受野远小于理论感受野；同一局部物理在一条轨迹里反复出现，所以单轨迹就够训练（p.7–8） | 结构 | 记忆化（问题表述层面：用局部算子降低记忆整场事件的能力） | 否 | 否（表述本身）；若实现为搬运头，则属于位移/形变族 | 无单独消融，没有和全局模型比较泛化，也没有报告训练/测试差距。只有 ERF 描述：10Δt/100Δt/1000Δt 的 ERF 为 9.7²/12.2²/19.0²，理论感受野为 19²/37²/79²（Tab.6 p.9，已渲染原表核对上标），面积利用率 26.0%/10.8%/5.8%（p.20 §E.6） |

- **撞车**：无（UOT 质量损失线（仅相关工作层面））— FluxNet 用结构硬守恒做无源 PDE 代理，不涉及 OT/UOT 损失、雷达或生成式临近预报，不会削弱我方 UOT 质量损失线的新意。写相关工作时一句话区分即可：它是结构性硬守恒，我方是允许质量生灭的软 UOT 损失；而且雷达反射率本身不守恒，硬守恒会压制增强和新生。与融合线、强度阈值阶梯线、条件流匹配临近预报线、SDIR 载体线：无撞车。论文没有雷达任务，也不引用 SDIR、DiffCast 等。
- **值得上机**：1/3 — 核心零件（硬守恒搬运头、容量约束上下界、大 stencil、pushforward）要么落在已关闭族（位移/形变、rollout），要么与增强/新生欠报方向相反，而且全部只有 PDE MAE 证据、没有 CSI。Tab.9 的 D 头结果（两分支平均且不加 DCL 最准，1.52 vs 2.79）来自共享骨干、联合训练的双头，DCL 的作用是换取边界满足，对我方跨范式融合只是极弱旁证，不构成独立零件。
- **是否引用/比较 SDIR**：不适用
- **核查记录（9 条，节选前 3 条）**：Tab.6（p.9）的感受野数字抄错了，原因是 PDF 抽文本时丢了上标。已按 200dpi 渲染图核对：原表 RF_theo 是 19²/37²/79²，ERF 是 9.7²/12.2²/19.0²（单位为像素面积），不是原记录里的 192/372/792 和 9.72/12.22/19.02。p.20 §E.6 可以印证：TRF=k+2B(k−1) 分别等于 19/37/79，ERF/RF 面积利用率为 26.0%/10.8%/5.8%。Tab.6 的 EMAE 2.76/2.16/8.39（×10^-2）和 R=1/2/4 无误。 ／ Tab.6 的混杂因素没写全：R 和步长一起变之外，三个模型的骨干也不同。10Δt 是 4 块、核 3，100Δt 是 4 块、核 5，1000Δt 是 6 块、核 7（p.19 §E.3）。 ／ in_out_frames 以偏概全。pushforward P=5 只用在浅水（p.15 §C.2）和交通流（p.17 §D.2.2、p.18 §D.3.3）；对流扩散明确是单步训练、不做 pushforward（p.14 §B.2）；旋节分解附录没写是否用 pushforward。测试外推到 2×，但对流扩散例外，在 T=1 评估（p.6）。

---
<a id="p260421097"></a>
### Learning to Emulate Chaos: Adversarial Optimal Transport Regularization

- **ID**：arXiv 2604.21097　**Venue**：ICML 2026 (PMLR 306)
- **Venue 依据**：PDF 第1页页脚："Proceedings of the 43rd International Conference on Machine Learning, Seoul, South Korea. PMLR 306, 2026."；页边注 arXiv:2604.21097v2 [stat.ML] 18 Jun 2026；代码仓库 README 有 ICML 2026 poster 链接
- **代码**：有代码（已 git clone 确认，main 最新提交 2026-06-26）。main 分支只有 L96：scripts/main.py、scripts/OT_utils.py（geomloss SamplesLoss(loss="sinkhorn", p=2, blur=self.blur)，平衡 OT，没有 reach/unbalanced 参数）、scripts/summary.py（SummaryNet + Critic）、models/fno_1d_new.py、eval_scripts、experiments/OT_l96/{sinkhorn,wgan}.sh。KS 在 ks 分支（有 summary_ks.py、单轨迹实验脚本），L63 在 l63 分支。kolmogorov-2d 分支只有一个占位文件 KOLMOGOROV_TODO.md（"2D Kolmogorov Flow — coming soon"），没有实际 2D 代码。README 开头标着 "Under Construction"。论文不评 CSI，所以不涉及 CSI 口径。（https://github.com/gabrielmelo00/LearningToEmulateChaos_ICML26）
- **阅读层级**：全文PDF　**核查结论**：corrected　**任务**：F
- **一句话**：非雷达论文，做混沌系统仿真器。在一步 MSE 之外加一项 λ·OT 距离（Sinkhorn 散度，或 WGAN 对偶形式）。这个距离算在对抗学习出来的逐格点统计量空间里：统计量网络 f 负责把预测和真值各自点云之间的 OT 距离拉大，仿真器负责把它压小。在 L96/KS/Kolmogorov 上，多数设置的直方图误差和谱误差明显改善；但可学习 OT 的一步 RMSE 在所有系统、两种评估下都比纯 MSE 差。全文没有 CSI。
- **CSI 口径**：数据集=Lorenz-96（60 变量；多轨迹：2000 条、每条 2000 步，F~U[10,18]；单轨迹：F=10）；Kuramoto-Sivashinsky（256 格点，单轨迹）；2D Kolmogorov flow（Re=10^4，χ=0.1，单轨迹，py2d 生成）；Lorenz-63（只做可视化，σ=0.1/0.15）。L96/KS/Kolmogorov 的训练数据加了 0.3·std 的高斯噪声；帧=一步仿真器 u_t→u_{t+1}；评估时从带噪初值自回归 rollout 1500 步（L96）或 1000 步（KS/Kolmogorov），测试集 200 条轨迹。L96/KS 训练用长度 100、步幅 2 的滑窗 teacher forcing（奇数步由模型预测，偶数步取真值）；Kolmogorov 只写了 teacher forcing，外加 MSE warm-start；分辨率=L96 60 维；KS 256 格点；Kolmogorov 网格分辨率没写明；刻度=物理场（L96/KS 的速度，Kolmogorov 的涡度），归一化方式没写明，不是雷达刻度；阈值=无 CSI/HSS。指标：L1 直方图误差（统计量固定用 Tab.1 里的手工统计量）、能谱相对误差（Kolmogorov 用径向平均）、一步相对 RMSE、最大 Lyapunov 指数（Tab.3）；聚合=不适用（无 CSI）。谱误差在 rollout 上做时间平均（附录 B，p.16）。Tab.2 报中位数；Tab.8/9 表注写 'median (25th, 75th percentile) over runs'，运行次数没写明。Noisy/Clean 两列是同一个带噪训练模型在带噪(σ=0.3)和干净(σ=0)测试轨迹上的评估（Sec.6 p.8）；出处=Sec.6 p.7-8；附录 B p.16；附录 D p.19-21；附录 E p.22

| 零件 | 去壳算子 | 插到我方 | 对准问题 | 越协议 | 已关闭族 | 单独消融证据 |
|---|---|---|---|---|---|---|
| 对抗可学习统计量 + Sinkhorn 散度正则（Eq.8） | 逐格点特征网络 f_ω（默认 3 层 MLP、宽 128；KS 可换 1D 卷积，Kolmogorov 用带圆周 padding 的 2D 卷积）把预测场和真值场分别映成点云（全部格点×时间步的 d 维向量，d=3），计算平衡 Sinkhorn 散度（geomloss，p=2；L96 写的是 ε=0.02，代码里传的是 blur=0.02）。总损失 = 逐点 MSE + λ·S；模型对 S 做 min，f 对 S 做 max，f 的权重做裁剪；L96 在 500 步中的第 350/400 步停掉 max 步（早停） | 训练损失 | 高阈值欠报（确定性载体把强度分布抹平）；其它:分布/统计量保真 | 否 | 加在流模型 x̂0 上就是已关闭族：x̂0 上的加权/感知/拓扑损失（对抗学习出的特征损失和感知损失同族）。加在 SimVP/SDIR 的确定性输出上属于边界情况：字面上不在关闭清单里，但和感知/对抗损失同源 | Tab.2 p.9，Kolmogorov 带噪评估：L1 直方图误差 No OT 0.660 → Sinkhorn(2D-Conv) 0.360，谱距离 0.249→0.144，但 RMSE 0.381→0.405（变差）。干净评估：L1 0.266→0.178，谱 0.205→0.129，RMSE 0.202→0.221（变差） |
| WGAN 对偶版（Eq.6）：统计量网络 + 1-Lipschitz critic | f 的输出后接 critic φ（2 层 MLP、隐藏宽 64、ReLU），critic 权重裁剪到 [-0.01,0.01]，统计量网络也做权重裁剪（p.20）。正则项 = E[φ∘f(真)] − E[φ∘f(预测)]；f 和 φ 做 max，模型做 min。先用 MSE 在同一数据上 warm-start，再打开 OT 项 | 训练损失 | 高阈值欠报；其它:分布保真 | 否 | 同上：用在流模型 x̂0 上属于已关闭的 x̂0 感知/对抗类损失；用在确定性载体上属边界 | Tab.2 p.9，Kolmogorov 带噪评估：L1 0.660→0.273，谱 0.249→0.157，RMSE 0.381→0.424（变差）。L96 多轨迹带噪评估：L1 0.348→0.149，谱 0.321→0.049，RMSE 0.388→0.401（也见 Tab.8 p.22）。Tab.3 p.9，L96 单轨迹最大 Lyapunov 指数：真值 2.334，WGAN 2.336，No OT 1.615 |
| 手工统计量的 Fixed-OT（Jiang 2023 基线）→ 迁移成逐像素 (dBZ, ∂t dBZ, ／∇dBZ／) 的联合分布匹配 | 用固定的物理统计量 S(u)（L96: {∂t u_i, 平流项, u_i}；Kolmogorov: {v·∇ω, ∂tω, ω}）把每个格点映成 3 维向量，对预测和真值的点云做 Sinkhorn（L96: ε=0.02，p=2），不做对抗。迁移到我方：换成（强度, 时间倾向 ∂t, 空间梯度）三元组，直接约束增强/新生像素的倾向分布（这是我方外推，论文没有这个实验） | 训练损失 | 高阈值欠报（增强/新生事件的 ∂t 正尾被截掉） | 否 | 否（本身不是分解；但加在流模型 x̂0 上会落进 x̂0 损失族） | Tab.2 p.9，Fixed OT 对 No OT 的结论不一致：Kolmogorov 带噪评估 L1 0.660→0.614、谱 0.249→0.168，RMSE 持平 0.381；KS 干净评估 L1 0.241→0.310（变差）；L96 多轨迹干净评估 L1 0.225→0.055 |
| 统计量输出维度 d | 统计量网络在每个格点输出的维度，比较 d=1 和 d=3 | 训练损失 | 其它:正则强度/表达力超参 | 否 | 否 | Tab.2 p.9 / Tab.8 p.22，L96 多轨迹带噪评估，WGAN：d=3 时 L1 0.149、谱 0.049、RMSE 0.401；d=1 时 L1 0.234、谱 0.205、RMSE 0.392 |
| 带空间感知的卷积统计量网络（circular padding），替代逐点 MLP | f 从逐格点 MLP 换成 1D/2D 卷积堆叠，让统计量带上邻域结构 | 训练损失 | 其它:统计量表达力 | 否 | 否 | 只有 KS 做了对比，结果不一致（Tab.2 p.9）。WGAN MLP 对 1D-Conv，带噪评估：L1 0.339 对 0.259，谱 0.148 对 0.198，RMSE 0.375 对 0.393；干净评估：L1 0.153 对 0.245。Kolmogorov 只报了卷积版，没有 MLP 对照 |
| 对抗 max 步早停（Sinkhorn 版的稳定化） | 训练后段停止更新统计量网络（L96 在 500 步中的第 350/400 步停），防止 f 退化 | 训练损失 | 其它:对抗训练稳定性 | 否 | 否 | Tab.4 p.17 只看了 L96 验证集上 f 的 Jacobian 谱范数。早停：中位数 0.400，最大 0.716；不早停：中位数 0.000，最大 5.762（塌缩）。没有给下游指标 |
| 统计量网络的双边 Lipschitz hinge 正则（Eq.23-25） | softplus hinge：上界取各层谱范数之积，要求不超过 Lmax；下界取 batch 子集上的平均 Jacobian 谱范数，要求不低于 Lmin（固定 0.01） | 训练损失 | 其它:对抗训练稳定性 | 否 | 否 | Tab.7 p.18，L96 WGAN（No regularization 一行与 Tab.2 L96 单轨迹 WGAN 一致）。Lmax=4 对不加正则，带噪评估：L1 0.176 对 0.151（变差），谱 0.133 对 0.145，RMSE 0.368 对 0.371；干净评估：L1 0.090 对 0.115，谱 0.144 对 0.175，RMSE 0.279 对 0.283。训练成本约增加 4 倍（p.18） |
| 按预报时效调整权重：长时效降低逐点损失权重，交给分布项（Eq.16） | L = Σ_k w_k·MSE^(k) + λ·W(f#μ, f#μ̂)，k 远大于混合时间时 w_k→0。对我方就是远时效帧（如 Shanghai 第 11-20 帧）降低像素损失权重、加大分布匹配权重 | 训练损失 | 高阈值欠报（远时效模糊） | 否 | 否 | 无单独消融。论文实际只取 w1=1、w_k>1=0（p.7），多时效加权方案本身没做实验 |
| 输入噪声等价于隐式 Jacobian 惩罚（Prop.5.7，我方外推） | 带噪 MSE = 干净 MSE + dσ1² + σ2²·E‖Dg‖_F² + O(σ2⁴)，即输入加噪相当于惩罚模型对输入的敏感度。论文里这只是理论分析的一部分，用来论证噪声下 MSE 在长时效会失效；并没有把它当作增强手段提出 | 增强 | 记忆化 | 否 | 否 | 无单独消融（只有理论，p.6；论文没有加噪训练对比不加噪训练的实验） |

- **撞车**：部分（UOT 质量损失线）— 弱重叠，而且看我方 UOT 具体怎么做：两边只是同属“逐点损失 + λ·OT 分布项”这个大框架。对方是平衡 Sinkhorn（代码确认 geomloss sinkhorn，没有 unbalanced/reach 参数），作用在对抗学习的逐格点特征空间，匹配的是全场×全时间点云的统计分布，不带空间位置，不管质量守恒和强度总量。如果我方 UOT 是空间上的质量搬运，基本不撞；如果是强度分布匹配，就更接近它的 Fixed-OT 基线（Jiang 2023），需要在 related work 里区分（非平衡、质量项、雷达强度）。它也提示一个扩展方向：在学出来的特征空间或（强度, ∂t）空间里做 UOT。和融合线、强度阈值阶梯线、条件流匹配临近预报线、SDIR 载体线都不撞车。
- **值得上机**：1/3 — 机制能搬到确定性载体（SimVP/SDIR）上，做成“强度-倾向联合分布”的 Sinkhorn 正则来打高阈值欠报，也在协议内。但证据全来自混沌玩具系统，没有雷达或 CSI 结果；可学习 OT 的一步 RMSE 在 Tab.2 所有系统、两种评估下都比 No OT 差（Kolmogorov 带噪评估 0.381→0.405/0.424），和 CSI 的逐点性质相冲突。用在流模型 x̂0 上又属于已关闭的 x̂0 感知/对抗损失族。唯一的 2D 实验（Kolmogorov）代码还没放出。对抗训练加 Lipschitz 控制的成本也不低（显式正则约 4 倍）；在 128×128×20 帧的点云上算 Sinkhorn 计算量也大。
- **是否引用/比较 SDIR**：否（非雷达领域论文，未引用也未比较 SDIR）
- **核查记录（12 条，节选前 3 条）**：数字核对：key_numbers 和 parts.ablation_evidence 里的数字逐个对过 Tab.2（p.9，另渲染 PNG 对齐了行列）、Tab.3（p.9）、Tab.4（p.17）、Tab.7（p.18）、Tab.8（p.22），全部能在原文找到，表号和页码也对，没有要删的数字。Tab.7 的 No regularization 一行（0.151/0.145/0.371/0.115/0.175/0.283）和 Tab.2 里 L96 单轨迹 WGAN 那行完全一致，行列对齐没问题。 ／ tasks 里有一个没有意义的条目 "F"，已删除。 ／ Noisy/Clean 两列的含义写错了：Sec.6 p.8 原文是 'Models trained on noisy data (σ = 0.3) are evaluated in two settings: clean rollouts (σ = 0) ... and noisy rollouts (σ = 0.3)'，也就是说两列是同一个带噪训练模型在两种评估轨迹上的结果，并不是训练噪声的消融。原记录写成“带噪/无噪”，容易被读成训练条件，已统一改为“带噪评估/干净评估”。

---
<a id="p260524041"></a>
### Iterative Refinement Neural Operators are Learned Fixed-Point Solvers: A Principled Approach to Spectral Bias Mitigation (IRNO)

- **ID**：arXiv 2605.24041　**Venue**：ICML 2026 Spotlight（依据是作者 GitHub README；PDF 本身没有录用注记）
- **Venue 依据**：用 arxiv_get.py 打开的是 v2（48 页）。PDF 首页只有 "arXiv:2605.24041v2 [cs.LG] 26 May 2026"，全文 grep 不到 ICML/Spotlight/Accepted。p17 有 Impact Statement 一节（ICML 格式要求），但不能算录用证明。GitHub README 第 9 行写 "**ICML 2026 · Spotlight**"，项目页 URL 为 xiaotianliu-dartmouth.github.io/IRNO_ICML_26_Spotlight。所以场地信息只来自作者自述。
- **代码**：有代码（已 git clone 核实，commit d5522dd，2026-05-25）。含 train.py、evaluate.py、config.yaml、models/model.py（U-Net 精修器，circular padding，输出头 xavier gain=0.1）、models/losses.py、utils/metrics.py（VRMSE）。losses.py：spatial_loss=F.mse_loss；progressive_spectral_loss 的 λ 从 1.0 线性增到 2.0，权重为 1+(／ω／/nyq)^λ 再除以均值；fixed_point_loss=mean(Φ(cat[x,y])^2)。train.py：基座在 torch.no_grad() 下出 h0，K 步精修之间没有 detach，loss=sum(step_losses)/K+fp_weight*loss_fp。限制：只放出了 Active Matter 流水线（config 基座为 polymathic-ai/FNO-active_matter，n_frames_input=4），没有 TR-2D、ERA5、WDSR、TFNO、RIGNO 的代码。默认配置（depth=4、LayerNorm、150 epoch、β_fp=0.01 配 5 epoch warmup）和论文 p35–36（3 级、BatchNorm、250 epoch）不一致。论文不做降水、不算 CSI，没有 CSI 口径可核。（https://github.com/xiaotianliu-dartmouth/Iterative_Refinement_Neural_Operator）
- **阅读层级**：全文PDF　**核查结论**：corrected　**任务**：F,G
- **一句话**：把预训练并冻结的算子输出当初值 h0，用一个共享权重的小 U-Net Φθ(x,h_k) 反复做残差修正 h_{k+1}=h_k+αΦ。训练用 K 步展开，加每步深监督、不动点正则 ／／Φ(x,y)／／² 和渐进谱损失；推理时可以迭代到超过训练步数。TR-2D/AM 上 FNO/TFNO 基座的 VRMSE 降 45–80%；ERA5 超分上 FNO 基座 RFNE 降 34%，强基座 WDSR 的 RFNE 只降 7.8%、ACC 只多 0.0013；AM 上较强的 HFS 基座仍降约 23%。增益随基座变强而缩小，全文没有降水或 CSI 证据。
- **CSI 口径**：数据集=TR-2D、Active Matter（都来自 The Well，4 帧→下一帧单步预测）；ERA5 16× 空间超分（SuperBench）；CE-Gauss 非结构网格（RIGNO，单步训练，7 步自回归评估）。没有雷达或降水临近预报数据集；帧=TR-2D/AM：4 帧输入→1 帧输出；ERA5：LR 45×90 → HR 720×1440；CE-Gauss：1→1，自回归 7 步；分辨率=TR-2D 384×128；AM 256×256；ERA5 720×1440；CE-Gauss 16384 节点；刻度=标准化的物理场（ERA5 用训练集统计量标准化，p33；代码中 AM 用 zscore）；不是 dBZ；阈值=无 CSI/HSS。指标为 VRMSE（TR-2D/AM）、ACC 和 RFNE（ERA5）、相对 L2%（CE-Gauss），定义在附录 G.5.1 p38–39；聚合=不适用（论文不算 CSI）。VRMSE=(<／u−v／²>/(<／v−v̄／²>+ε))^{1/2}，<·> 是在所有空间点和输出通道上取平均（p38）；出处=p9–10 §4.1；p32–33 附录 G.1；p38–39 附录 G.5

| 零件 | 去壳算子 | 插到我方 | 对准问题 | 越协议 | 已关闭族 | 单独消融证据 |
|---|---|---|---|---|---|---|
| P1 冻结基座 + 共享权重残差精修器做不动点迭代 | h0=T_base(x)（冻结，no_grad）；h_{k+1}=h_k+α·Φθ(cat[x,h_k])。Φθ 为轻量 U-Net（论文写 3 级、BN+GELU；仓库默认是 depth=4、LayerNorm），同一套权重迭代，训练展开 K=4 或 6 步，α=0.2（TR-2D/AM）或 0.25（ERA5）。梯度穿过整条轨迹 | 测试期 / 读出（接在冻结的 SDIR、SimVP 或 FlowCast 均值输出后做后处理精修；也可以把融合后的输出当 h0） | 高阈值欠报（确定性/均值输出平滑、强核被抹）；其它：单模型输出细节不足 | 否（只用同一训练集上基座的预测和标签） | 否。注意：它和 SDIR 的级联精修同构；K 步展开是在精修迭代轴上，不是时间 rollout，不属于已关闭的 rollout/self-forcing 族，但两者边界相邻 | Tab.1 p10：TR-2D FNO 0.2394→0.1309（45.32%），TFNO 0.2371→0.1042（56.05%）；AM FNO 0.1017→0.0501，TFNO 0.1981→0.0387；ERA5 FNO ACC 0.7523→0.8919、RFNE 0.3247→0.2140，WDSR ACC 0.9091→0.9104、RFNE 0.2119→0.1953（7.83%）。注意：Tab.1 的 TR-2D TFNO 值 0.1042 等于 Tab.19 p44 BatchNorm 在 k=8 的值，k=4 时为 0.1102，而 Tab.1 标题写 K=4，报告步数存疑。和接近算力的一次性网络对照（Tab.15 p40，ERA5 FNO 基座）：IRNO k=5 为 83.58/27.13 @1095.31 GFLOPs，k=6 为 84.55/26.32 @1296.83；FNO+UNet(×15) 为 78.82/29.80 @1131.39（37.3M 参数）。FNO+FNO 76.50/32.13 只有 175.72 GFLOPs，不算算力相当。另外 Tab.15 的 k=6 ACC 84.55% 和 Tab.1 同基座的 0.8919 对不上，是另一次较弱的运行。叠加在已有频率处理的强基座上（Tab.17 p43，AM）：HFS 0.0631→k=6 0.0486（约 23%）。换精修骨干（Tab.18 p43，AM TFNO，初值 0.1946）：ResNet 0.0430、ConvNext 0.0438、FNO 0.0559，都不如 U-Net 的 0.0387。归一化（Tab.19 p44，TR-2D TFNO，k=8）：BN 0.1042、LN 0.0988、GN 0.1006。要点：增益随基座变强而缩小（AM 80%→51%→23%，ERA5 RFNE 34%→7.8%），但没有归零 |
| P2 测试期迭代次数外推 + 步长 α + 按 ／／Φ／／ 自适应停止 | 训练时展开 K 步，推理时跑 k>K 步（主实验到 2K，Fig.18 到 8K）；α 控制收缩性，q=／／I−αA／／<1，α 过大会发散。作者建议 α 随 k 递减，或在 ／／Φθ(x,h_k)／／ 低于与偏置 α／／b／／/(1−q) 挂钩的阈值时停止（p42） | 测试期 / 采样（在验证集上调迭代步数和 α，用 CSI 当停止判据） | 高阈值欠报（多迭代可以继续补高频和强核） | 否 | 否 | Tab.15 p40（ERA5 FNO，训练 K=6）：k=6 为 ACC 84.55 / RFNE 26.32，k=12 为 87.38 / 23.45，k>K 仍单调变好。但这组运行的绝对值和 Tab.1 对不上（见 P1）。p15 §4.7.3 正文（TR-2D TFNO，K=4）：α=0.1 稳但慢；α=0.2 收敛最快、外推稳定；0.3–0.5 在 k>6 后发散；α=0.6 在训练步数内就发散，VRMSE 从 0.12 升到 0.24。p16 Fig.7 图注说法不同（α=0.6 是在训练步数外发散，[0.2,0.4] 最优），图注和正文互相矛盾。p42 正文 / p47 Fig.18（AM FNO，K=6）：α=0.20 时约 k≈14 达到最低约 0.0505，k=48 发散到 0.0806；α=0.05 全程不发散。自适应停止没有实验。没有 CSI 类证据 |
| P3 多步深监督（轨迹上每步都算 L2） | L_spatial=(1/K)Σ_k／／h_k−y／／²，每个中间迭代结果都监督（代码为 F.mse_loss） | 训练损失（精修器的训练） | 其它：精修器迭代稳定性（多迭代不发散） | 否 | 否 | 没有基于误差指标的单独消融。只在 1-D 合成问题上做了单调性诊断（Tab.7 p29）：完整 IRNO 的 m=0.661±0.060，100% 样本 m>0；去掉深监督后 m=−0.893±0.989，只有 3.9% 样本 m>0 |
| P4 不动点正则 L_fp=／／Φθ(x,y)／／² | 把真值 y 当作当前估计喂给精修器，要求输出零修正，也就是让真值成为学到的动力系统的不动点，从而压低偏置 b 和误差下限 α／／b／／/(1−q)。代码中 β_fp=0.01，前 5 epoch 线性 warmup | 训练损失（精修器） | 高阈值欠报（防止精修器把本来正确的强核再往均值拉）；其它：精修器的系统偏置 | 否 | 否（这不是 x̂0 上的加权/感知损失，约束的是精修器在 GT 处的输出） | 没有单独的开/关消融。只有相关性证据（Fig.3 p8）：偏置 ／／b／／ 和最小误差的 Pearson r，AM 为 0.933、TR-2D 为 0.949（k=24 步） |
| P5 跨基座迁移：用较弱基座的残差训练精修器，再加到较强基座上 | 用基座 A 的预测残差训练出的 Φθ，不重训直接接到基座 B 上。作者的解释是：弱基座的残差更大、更多样，精修器学到的纠错更鲁棒 | 训练数据构造 / 融合（一个精修器同时服务 SDIR、SimVP、FlowCast 或它们的融合输出；训练时用早期 checkpoint、较弱模型或 out-of-fold 预测做 h0，扩大残差分布） | 记忆化（我方基座在训练集上被记忆化，训练残差被压得很小，同基座训练的精修器学不到测试期那种残差）；测试漂移；融合 | 否（前提是所有基座只用同一训练集训练） | 否 | Tab.4 p14 对比 Tab.1 p10。弱训练→强施加：AM 上 FNO+IRNO_TFNO 0.1004→0.0445，优于同基座的 0.0501；ERA5 上 WDSR+IRNO_FNO ACC 0.9219，优于同基座 0.9104，RFNE 0.1935 优于 0.1953。强训练→弱施加都变差：AM 上 TFNO+IRNO_FNO 0.1127，同基座为 0.0387；ERA5 上 FNO+IRNO_WDSR ACC 0.8022，同基座为 0.8919。TR-2D 两个基座几乎一样强（0.2394 vs 0.2371），用 TFNO 训练的精修器在两个基座上都更好（0.0994 与 0.1042），和‘弱基座残差更好’的解释并不吻合。论文没有直接做‘训练残差被记忆化’的实验；对我方记忆化问题只是间接启发 |
| P6 精修器条件里再拼上冻结基座的初始预测 h0 | CE-Gauss 变体中精修器输入为 cat[h_k, h0]（8 通道），输出 4 通道修正，在归一化解空间里更新。这里的精修器是和基座同构的 RIGNO，不是小 U-Net；K=4、α=0.3；只训练单步，自回归时每步零样本套用（p33） | 条件 / 融合（可推广为 cat[x, h_k, h_gen, h_simvp]，让精修器学像素级融合和修正，替代均值/逐像素 max 融合） | 融合 | 否 | 否 | 没有单独消融。只有 CE-Gauss 整体结果（Tab.5 p14）：RIGNO 在 t=7 的相对 L2 从 16.617% 降到 13.080%（21.3%），t=1 时为 3.351→2.931（12.5%）。多基座输入融合论文没有测过 |
| P7 渐进谱损失（λ_k 在迭代步间线性从 1 增到 2） | 频域幅值误差按 1+(／ω／/nyq)^λ_k 加权，后面的步更重视高频 | 训练损失 | 高阈值欠报（细节） | 否 | 是：谱损失（已关闭族） | 已关闭族：谱损失（Tab.6 p15、Tab.12 p37，不展开） |
| P8 输出头小增益初始化 | 精修器最后一层 1×1 卷积用 Xavier uniform（gain=0.1），偏置置 0，让初始修正接近零、迭代一开始是温和的（代码 model.py 已核实） | 结构（实现细节） | 其它：精修训练稳定性 | 否 | 否 | 无单独消融（只在 p35 有描述） |

- **撞车**：部分（SDIR 载体线（与融合线弱相关））— IRNO 的核心是先有粗预测，再用精修器由粗到细修残差，和 SDIR 的频率截断级联精修同构；它的渐进谱损失本质上是一套频率课程，属于已关闭族。区别在于：IRNO 是一个共享权重的迭代算子，步数可以外推，另外加了不动点正则，并且基座冻结、可以跨基座迁移；SDIR 是按频率截断分阶段的确定性级联。和强度阈值阶梯线、UOT 质量损失线、条件流匹配临近预报线都不撞。和融合线只在‘精修器以多模型输出为条件’这种推广上有交集，论文本身没有做多模型融合。论文不涉及降水或雷达，没有引用 SDIR。
- **值得上机**：1/3 — 协议内，实现也便宜：冻结基座加约 2–9M 参数的小 U-Net（Tab.11 p36），训练 K=4 展开的开销约为 K=1 的 2.69 倍（Tab.13 p37）。有接近算力的一次性网络对照（Tab.15 p40）和跨基座迁移（Tab.4 p14），但全部是 PDE 或 ERA5 超分上的 VRMSE/ACC/RFNE，没有 CSI 类证据，而且论文内部 Tab.1、Tab.15、Tab.19 的数字有不一致。主机制和 SDIR 级联精修重叠。增益随基座变强而缩小：WDSR ACC 只多 0.0013、RFNE 降 7.8%；AM 上 HFS 基座降约 23%。我方有严重记忆化，用同一训练集的基座预测训练精修器，看到的残差会远小于测试期，除非改用 out-of-fold 或较弱 checkpoint 的预测（P5 只有间接支持）。可以作为‘融合输出 → 小精修器（带 L_fp、以 h_gen/h_simvp 为条件）’的低优先级试验。
- **是否引用/比较 SDIR**：否
- **核查记录（10 条，节选前 3 条）**：数字核对：key_numbers 和 ablation_evidence 里的数字都在原文找到了，逐项对上：Tab.1 p10、Tab.4 p14、Tab.5 p14、Tab.6 p15、Tab.7 p29、Tab.11 p36、Tab.13 p37、Tab.15 p40、Tab.17/18 p43、Fig.3 p8、p15 §4.7.3 正文、p42 正文。表号和页码没有错。 ／ Tab.15 里的‘算力相当对照’写得不准。FNO+FNO 只有 175.72 GFLOPs，远低于 IRNO K=6 的 1296.83，算力并不相当。能算作接近算力的只有 FNO+UNet(×15)，1131.39 GFLOPs、37.3M 参数。更公平的比法是同算力点 IRNO k=5（1095.31 GFLOPs）ACC 83.58 / RFNE 27.13，对 UNet×15 的 78.82 / 29.80。论文 p10 说基线‘参数和 FLOPs 都超过 IRNO’，这一点对 FNO+FNO 不成立。 ／ 论文内部数字对不上，原记录没提。(a) ERA5 FNO 基座：Tab.1 的 IRNO ACC 是 0.8919、RFNE 0.2140，Tab.12 渐进 λ 也是 0.892 / 0.214；但 Tab.15 同一基座 k=6 只有 84.55% / 26.32%，k=12 为 87.38% / 23.45%，两个都对不上 Tab.1。所以 Tab.15 和 Fig.8 的成本曲线（包括 k>K 外推的证据）来自另一次较弱的运行。(b) Tab.1 标题写 TFNO 在 K=4 评估，但 TR-2D TFNO 的 0.1042 / 56.05% 恰好等于 Tab.19 BatchNorm

---
<a id="p260904995"></a>
### Beyond Homoscedasticity: Decoupled Uncertainty Optimization for Deep Imbalanced Regression (DUO)

- **ID**：arXiv 2609.04995；DOI 10.1145/3767308.3835898　**Venue**：ACM MM 2026 (34th ACM International Conference on Multimedia)
- **Venue 依据**：PDF p.1 页脚注记："Accepted at the 34th ACM International Conference on Multimedia (ACM MM 2026). The Version of Record will be available at https://doi.org/10.1145/3767308.3835898." 附录首页 p.10 有相同注记。arXiv v1，4 Sep 2026，cs.LG。
- **代码**：无
- **阅读层级**：全文PDF　**核查结论**：corrected　**任务**：F
- **一句话**：长尾回归论文（年龄/深度/蛋白，非气象）。做法：把逐样本回归损失乘上动态难度权重 γ=1+w(y)·sg[σ̂(x)]，其中 w 是训练集标签分箱的逆频率，σ̂ 是一个不确定度头，用对 μ 做 stop-grad 的 Gaussian NLL 训练；另加一个按 Bhattacharyya 重叠划分正负对的对比损失。论文没有 CSI，PDF 里没有代码链接，GitHub 搜不到仓库。
- **CSI 口径**：数据集=AgeDB-DIR、IMDB-WIKI-DIR（年龄，标量回归）；NYUD2-DIR（稠密深度图，唯一的像素级任务）；AAV2-DIR（蛋白适应度，输入用 ProtBERT 预训练嵌入，App.F.3 p.18）。没有雷达/降水数据；帧=不适用（单帧回归，不做时序外推）；分辨率=年龄任务输入 224×224（App.F.1 p.17）；NYUD2 输出 152×114（App.F.2 p.18）；刻度=不适用（NYUD2 深度单位未写明；w(y) 的分箱宽度和逆频率是否归一化也未写明）；阈值=不适用：没有 CSI。指标为 MAE/bMAE/GM；NYUD2 用 RMSE/MAE/bMAE/δ1（max(d/g,g/d)<1.25）。结果按训练样本数分箱报告：Many(>100)/Median(20–100)/Few(<20)（Sec.4.1 p.6）；聚合=不适用（没有 CSI）。NYUD2 的训练损失逐像素计算、在有效像素掩码内平均（App.F.2 p.18）；评估指标（RMSE 是逐图还是池化）未写明；出处=PDF Sec.4.1 p.6；App.F p.17–18

| 零件 | 去壳算子 | 插到我方 | 对准问题 | 越协议 | 已关闭族 | 单独消融证据 |
|---|---|---|---|---|---|---|
| P1 动态难度权重 γ=1+w(y)·sg[σ̂]，乘在逐像素回归损失上（DUO 核心） | L_mean=(1+w(y)·sg[σ̂(x)])·½(y−μ̂)²（式6 p.4）。w(y) 是训练集标签分箱的逆经验频率（App.F p.17），换到我方就是 dBZ 分箱逆频率，强回波像素权重大。σ̂ 做 stop-grad，只在前向当权重用。稠密任务逐像素计算、在掩码内平均（App.F.2 p.18）。SDIR 用像素 MAE，可直接改成 γ·／y−μ／。注意论文没给分箱宽度和 w 的归一化方式，用在 dBZ 上需要我们自己做归一化或截断，防止强端权重爆掉 | 训练损失 | 高阈值欠报；测试漂移（测试集强核占比 .163 高于训练 .113，逆频率加权相当于把训练强度先验拉平）。注意：论文前提是尾部欠拟合，而我方训练 CSI@40 已达 0.64–0.70，问题是记忆化，加权可能加重记忆化 | 否（只用训练集强度直方图） | 是（部分）:x̂0 上的加权损失。加在 FlowCast 的速度/x̂0 损失上属已关闭族；加在确定性 SDIR/SimVP 像素损失上是 B-MSE/B-MAE 类强度加权的变体，未被明确关闭 | Tab.S1 p.19（来自 App.F.4 rebuttal 补充）：去掉权重的 DUO(NoWeight) few-shot MAE 为 IMDB 24.560 / AgeDB 12.226 / AAV2 4.109，完整 DUO 为 22.940 / 9.917 / 4.106。Tab.3 p.8：DUO-C（去掉对比项，保留加权+解耦）对 Vanilla 为 23.332/10.787/4.193 vs 25.546/13.381/7.365。稠密任务 NYUD2 没有组件消融。Tab.S8 p.20 对 Vanilla：Overall RMSE 0.597 vs 0.580、MAE 0.396 vs 0.371、δ1 0.781 vs 0.807 变差，Many RMSE 0.497 vs 0.457 变差；Few RMSE 1.759 vs 1.867、Median RMSE 0.777 vs 0.795、Overall bMAE 0.990 vs 1.051 改善。对纯逆频率加权基线 LDS（Tab.S8 p.20）：Overall RMSE 0.597 vs 0.633、δ1 0.781 vs 0.737、Many RMSE 0.497 vs 0.546，Few 1.759 vs 1.764 持平。即 σ̂ 调制后的加权在尾部增益相同的前提下，头部代价小于纯逆频率加权。所有表都不报标准差 |
| P2 解耦的异方差 σ̂ 头（方差损失对 μ 做 stop-grad） | 加一个输出通道 σ̂=g+(Wσz+bσ)（式3 p.4），损失 L_var=β[(y−sg[μ̂])²/(2σ̂²)+½log σ̂²]，β=1（式7 p.5，App.F）。驻点是 σ̂²=(y−μ̂)²，所以 σ̂ 实际上是一个学出来的逐像素残差预测器，给 P1 当难度信号 | 结构 | 高阈值欠报（给 P1 提供实例难度）；也可当逐像素置信度用于融合（见 P6，论文没做） | 否 | 否（如果拿 σ̂ 做事后概率校准，就落入已关闭的事后概率校准族） | Tab.S2 p.19：用瞬时绝对残差替代学到的 σ̂，few-shot MAE 为 IMDB 24.37 / AgeDB 12.00 / AAV2 4.15，DUO 为 22.94 / 9.92 / 4.11。解耦本身没有被干净地单独隔离。Tab.S1 p.19 中 Decoupled+BC(NoWeight) 对比 Joint NLL+BC：few MAE 在 IMDB 24.560 vs 24.295、AgeDB 12.226 vs 11.916 略差，AAV2 4.109 vs 4.160 略好；bMAE 在 IMDB/AgeDB 略差（31.821/14.968 vs 31.334/14.347）；GM 三个数据集都更好（16.306/8.447/3.815 vs 16.775/8.762/3.823）。结果互有胜负。“联合 NLL+权重+BC”这一直接对照没有报告，所以论文“去掉解耦会变差”的说法缺直接证据 |
| P3 问题表述：NLL 梯度耦合使大残差（尾部）样本的均值梯度 ≈1/／y−μ／ 趋于 0 | 诊断算子。凡是带学习方差或不确定度加权的损失（Kendall 式、异方差 NLL），σ̂² 会被推向残差平方，强回波这类大残差像素的均值梯度随之被压小，模型靠抬高方差而不是修正均值来降损失（Sec.3.4.1 p.4）。结论：难度信号应该放大梯度，不该缩小梯度 | 训练损失 | 高阈值欠报（用来自查：我方如有任何学习方差或不确定度加权项，强核像素的梯度可能被削弱） | 否 | 否 | Tab.S1 p.19：双头联合 NLL 对比单头 MSE Vanilla，few MAE 为 IMDB 25.842 vs 25.546、AgeDB 14.280 vs 13.381、AAV2 7.880 vs 7.365；AgeDB few GM 15.850 vs 9.644。Tab.S5 p.20：IMDB Overall MAE NLL 8.094 vs Vanilla 7.829 |
| P4 逆频率权重先做 LDS 高斯平滑 | 训练集标签直方图先用高斯核（kernel 9，σ_lds=1）平滑，再取倒数当权重（App.F.1 p.17 和 F.2 p.18 训练配置原文：“Label smoothing is applied using a Gaussian kernel (kernel size 9, σlds = 1), and the re-weighting scheme uses inverse frequency weighting”）。这一步是否用于 DUO 自己的 w(y)，原文语义有歧义（Eq.6 处只说“inverse empirical frequency of the bin”）。换到我方就是对 dBZ 直方图做平滑，避免稀有强度 bin 的权重爆掉 | 训练损失 | 高阈值欠报 | 否 | 是（部分）:与 P1 相同（加在生成模型上即 x̂0 加权族） | DUO 内部没有单独消融。只有 LDS 作为基线的数字：Tab.S8 p.20 NYUD2 上 LDS Few RMSE 1.764 vs Vanilla 1.867，但 Overall RMSE 0.633 vs 0.580、Many RMSE 0.546 vs 0.457（头部明显变差，比 DUO 更差） |
| P5 分布引导的对比学习（BC 重叠划分正负对 + 距离感知 InfoNCE）+ warm-up 调度 | 每个样本取代理分布 N(y_i, σ̂_i²)；稠密任务用目标空间均值和 σ̂ 空间均值（App.F.2 p.18）。按闭式 BC（式10 p.5）划分，BC≥δ=0.5 为正对；InfoNCE 温度 τ=0.07，负对推开权重 W_push=w(y_i)·BC⁻¹（式12–13 p.5），push 系数 η=0.01。λ_align 在 NYUD2 为 0.1、其余为 1.0（App.F p.17–18；与正文 p.8“λ=1.0 用于后续所有实验”矛盾）。前 T_w 个 epoch（年龄 15，NYUD2/AAV2 10）只训 L_mean+L_var，之后才开对比项。换到我方大致是：按事件平均 dBZ 或强核占比，在骨干/潜特征上加事件级对比正则 | 训练损失 | 其它:尾部特征纠缠。映射到我方是按事件强度的潜特征正则，但论文没有给出关于记忆化或泛化的证据；3.9 亿参数模型的小 batch 对 InfoNCE 不利 | 否 | 否（内部对比，不是 REPA 类外部先验） | Tab.3 p.8：DUO-C→DUO，few MAE 为 IMDB 23.332→22.940、AgeDB 10.787→9.917、AAV2 4.193→4.106。Tab.S1 p.19：BC 换成 ConR 后 IMDB 23.996、AgeDB 11.565（DUO 为 22.940 / 9.917）。Tab.S2 p.19：BC 换成 interval IoU 后 AgeDB 11.40。Tab.S4 p.19 warm-up 敏感性（AgeDB，表头只写 MAE；9.917/11.590 与 few-shot 值一致，推断为 few-shot）：T_w=5 为 11.324、15 为 9.917、20 为 10.602、50 为 10.702。波动大，论文称之为“stable U-shaped”并不准确。NYUD2 上没有单独消融 |
| P6（我方外推，论文未做）σ̂ 逐像素门控融合 / 用 DUO 加权训练的 SimVP 当融合伙伴 | 思路一：给 SimVP 或 SDIR 加 P2 的 σ̂ 头，把 σ̂ 当逐像素置信度，做 1/σ̂² 加权或按 argmin σ̂ 选择，与生成模型融合（FlowCast 侧可以不加头）。思路二：用 P1 加权训练出的 SimVP 替换现有 SimVP 伙伴，让伙伴的误差剖面偏向强回波，拉开两者的相关（当前逐事件 r=0.91） | 融合 | 融合；高阈值欠报 | 否 | 否 | 无单独消融：论文不涉及模型融合。Tab.4 p.8（AAV2）以及 Tab.S9–S11 p.21 的“X+DUO”是损失叠加，不是模型融合，且结果互有胜负。NYUD2（Tab.S11）：LDS+DUO Few RMSE 1.767 vs LDS 1.764（无增益）；FDS+DUO Few 1.845 vs 1.905，但 Overall 0.607 vs 0.589（变差）。IMDB（Tab.S10）：LDS+DUO few MAE 24.378 vs LDS 22.755（变差），FDS+DUO 24.471 vs 24.347（变差），三组 Overall 全部变差 |

- **撞车**：无（融合线 / 强度阈值阶梯线 / UOT 质量损失线 / 条件流匹配临近预报线 / SDIR 载体线）— 这是非气象的长尾回归论文，不涉及融合、生成模型、强度截断阶梯、质量守恒或频率级联，与我方五条主线都不撞。它对标签空间分箱只是为了算逆频率权重，跟我方的 min(y,τ) 强度截断读出不是一回事。唯一的重叠在已关闭族：核心加权（P1/P4）如果加在 FlowCast 的 x̂0/速度损失上，就属于已关闭的“x̂0 上的加权损失”。
- **值得上机**：1/3 — 机制方向与高阈值欠报对得上，也在协议内，在 SimVP/SDIR 上加一个 σ̂ 通道和一个加权损失即可实现。扣分点：(1) 论文前提是尾部欠拟合，我方训练 CSI@40 已达 0.64–0.70，问题是记忆化，按逆频率上调强核权重可能加重记忆化。(2) 唯一的稠密任务 NYUD2 上，相对 Vanilla，DUO 的 Overall RMSE/MAE/δ1 和 Many 区都变差（Tab.S8 p.20：0.597 vs 0.580，δ1 0.781 vs 0.807，Many 0.497 vs 0.457），只有 Few、Median 和 bMAE 改善。对应到我方，很可能 CSI@20/30 变差、抵消 CSI@40 的收益，CSI-M 不一定涨。(3) 组件消融只在标量任务上做过，不报方差；正文说五次平均，附录说固定 seed 42；还有内部矛盾（Tab.S3 中 DUO AgeDB few MAE 为 11.79，Tab.1 为 9.917；λ 取 1.0 还是 0.1 前后不一）；损失叠加在 IMDB 上反而变差（Tab.S10）。唯一对我方有用的正面读数：同样做强度加权时，γ=1+w·sg[σ̂] 比纯逆频率加权的头部代价小（NYUD2 Overall RMSE 0.597 vs LDS 0.633，Few 相当）。因此如果要在确定性伙伴 SimVP/SDIR 上试强度加权，优先用这种 σ̂ 调制形式，只作为融合伙伴的低成本变体（P1+P2 或 P6）试一次。
- **是否引用/比较 SDIR**：不适用
- **核查记录（14 条，节选前 3 条）**：All 19 key_numbers matched the PDF text: Tab.3 p.8, Tab.S1/S2/S4 p.19, Tab.S5/S6/S8 p.20, Tab.S11 p.21, Tab.2 p.7, App.F.1/F.2 p.17-18. Numbers, table IDs and pages are correct. Tab.S1-S4 come from App.F.4 'Additional Analyses from the Rebuttal' (p.18); provenance note added. ／ Error in P1 ablation_evidence: it said 'only Few RMSE improves' on NYUD2. Tab.S8 p.20 shows DUO also improves the Median bin (RMSE 0.777 vs 0.795, MAE 0.586 vs 0.596, bMAE 0.653 vs 0.673, delta1 tied 0.668) and Overall bMAE (0.990 vs 1.051). The degradation is limited to Overall RMSE/MAE/delta1 and  ／ Important comparison missing: on NYUD2, DUO vs the LDS baseline (inverse-frequency reweighting). Tab.S8 p.20: Overall RMSE 0.597 vs 0.633, delta1 0.781 vs 0.737, Many RMSE 0.497 vs 0.546, Few RMSE 1.759 vs 1.764. Pure inverse-frequency weighting hurts the head more. The sigma-hat-modulated weight ge

---
<a id="p260905506"></a>
### ECHO: Dyadic 3D Facial Motion Generation with Asymmetric Deterministic Articulation and Stochastic Reaction

- **ID**：arXiv 2609.05506；DOI 10.1145/3767308.3834964　**Venue**：ACM MM 2026（MM '26，Rio de Janeiro）；PDF 未写明是否为 poster
- **Venue 依据**：PDF 第 1 页页脚写有 "MM '26, Rio de Janeiro, Brazil © 2026 Copyright held by the owner/author(s). ACM ISBN 979-8-4007-2213-4/2026/11 https://doi.org/10.1145/3767308.3834964"，ACM Reference Format 写 "Proceedings of the 34th ACM International Conference on Multimedia (MM '26), November 10–14, 2026"，每页页眉都有 "MM '26, November 10–14, 2026, Rio de Janeiro, Brazil"。arXiv 标注 v1 [cs.GR]，29 Aug 2026。PDF 中没有 poster/oral 字样，未核实。
- **代码**：无
- **阅读层级**：全文PDF　**核查结论**：corrected　**任务**：F
- **一句话**：领域是二人对话 3D 面部动作生成，不是雷达，没有 CSI 指标。做法分四块：确定性锚回归（L1）；只对锚残差（标准化后）做流匹配；按组可学习的残差缩放 α 加融合端点 L1 监督；推理期只在早期注入退火噪声，训练后期再加冻结 VQ 原型正则。其中“确定/残差”分解本身属已关闭族，结构与 DiffCast 同构。我方可以直接试的只有两个零训练代价的旋钮：一是可学习的分组融合系数 α，是我方均值融合的推广；二是推理期早期退火噪声，用来拉开同模型采样。但两者都没有任何 CSI 证据，退火噪声的消融也没报告单输入采样多样性（Div）。
- **CSI 口径**：数据集=从 Seamless-Interaction 派生的基准：约 120 小时二人对话，54 维 FLAME 系数，30 FPS，按说话人不相交 8:1:1 划分（p5 §5.1）；维度拆分为 50 表情 + 1 下颌 + 3 颈部（p3 式(1)）。与雷达无关。；帧=段级离线生成：输入整段双流音频，输出整段 T×54 动作序列（p3 式(1)，p5 §5.1）；多样性评估每个输入采样 N=8 个（p6）；分辨率=不适用；刻度=不适用（FLAME 参数系数，不是像素）；阈值=不适用：没有 CSI/HSS 等阈值指标。指标为 MSE、STS、FD、P-FD、rPCC、SID、局部 FD/P-FD、Div、RTF（p6 Metrics 段），另有 LISTEN 段的 Mode-JSD/Mode-Entropy/Trans-JSD（Tab.2）；聚合=不适用（无 CSI）；出处=PDF p3 §3 式(1)；p5 §5.1 'Data and protocol'；p6 'Metrics'、'Implementation details'

| 零件 | 去壳算子 | 插到我方 | 对准问题 | 越协议 | 已关闭族 | 单独消融证据 |
|---|---|---|---|---|---|---|
| P1 确定性锚 + 残差流匹配（锚参考 detach、残差标准化） | 确定性网络 b=AN(f_s, c_o) 用 L1 回归 y。条件流匹配只学标准化残差 Z(y−b_ref)，插值为 x_τ=(1−τ)x0+τ·Z(y−b_ref)，速度场以 b 为条件。输出 ŷ=b+S_α(Z⁻¹(x1))。锚先 warm-up，再与残差分支、α 联合训练；残差目标用 detach 过的锚参考 b_ref，以免追移动目标（p4 式(3)(4)(5)，p5 §4.3 式(8)(9)） | 结构 / 融合（拿 SimVP 或 SDIR 当锚，FlowCast 只学残差） | 融合：把事后像素融合改成联合训练的结构；高阈值欠报：锚会塌到均值，靠残差补回 | 否（原文锚分支用外部预训练 HuBERT，见 P6；算子本身在协议内） | 是：已关闭族：提出新分解（确定/残差）本身；另外与对比基线 DiffCast（确定性骨干 + 残差扩散）同构 | Tab.3 p7（FD ×10⁻²，P-FD ×10⁻³，面部分布指标，不是 CSI）：完整模型 SID 4.062 / FD 2.805 / P-FD 0.587；w/o anchor modeling 为 1.260 / 13.507 / 2.872；w/o residual refinement 为 1.532 / 5.521 / 1.264 |
| P2 按组可学习残差缩放 α + 组间序约束先验 | ŷ=b+[α_e·r̂_e, α_j·r̂_j, α_n·r̂_n]：每个语义组（表情/下颌/颈部）一个可学习标量，再用 hinge 序约束 L_ord=max(0,α_j−α_e)+max(0,α_e−α_n)，即 α_j≤α_e≤α_n。α 通过 L_fuse 与网络联合学习。收敛后约为 α_exp≈0.99、α_jaw≈0.64、α_neck≈0.99，只有下颌被明显压缩（p4 式(7)，p5 式(11)，p6）。迁移到我方：现在的均值融合相当于 SimVP+0.5·(Flow−SimVP)，可把 0.5 换成按预报时效或按强度段（<30 / 30–40 / ≥40 dBZ 区域）分组的 α，并加单调先验 | 融合 / 读出 | 融合 | 否：前提是 α 在训练集切出的留出子集上拟合。训练集上生成模型记忆化严重（训练 CSI@40 0.64–0.70），不能用训练内样本拟合；用测试集调 α 就越协议 | 否 | Tab.3 p7：w/o semantic-group scaling 为 SID 3.740 / FD 3.204 / P-FD 0.738，局部 P-FD 下颌 1.305（×10⁻³）、颈部 1.26（×10⁻⁴）；完整模型 4.062 / 2.805 / 0.587，下颌 0.933、颈部 0.95。原文没说明这一消融怎么实现（α 是否固定为 1）；L_ord 随之失效是推断。没有“可学习 α vs 固定 α”的单独对比 |
| P3 融合端点代理监督 L_fuse + 残差时间 TV | 训练时用一步外推 r̃=Z⁻¹(x_τ+(1−τ)v_τ)，与锚按 α 融合 ỹ=b+S_α(r̃)，对 GT 取 L1；再对 r̃ 加时间梯度 L1（TV）。总损失为 L_base=L_anchor+λf·L_flow+λu·L_fuse+λo·L_ord+λr·‖∇t r̃‖1（p5 式(10)(11)(13)） | 训练损失 | 融合：让锚、残差和 α 端到端对准融合后的输出 | 否 | 是（边界）：属于 x̂0 上的损失，接近已关闭族：x̂0 上的加权/感知/拓扑损失（这里是普通 L1 加时间 TV） | 无单独消融 |
| P4 推理期早期退火噪声注入（沿时间 OU 相关） | 积分的每步更新写成 x_{τ+Δτ}=x_τ+v_τΔτ+η0(1−τ)^p·√Δτ·ε_τ，初始强度 0.8、衰减幂 1.2；噪声随流时间衰减到 0，只在早期步起作用；噪声沿时间 OU 相关，系数 0.98；默认 4 步 RK4；只在推理期使用，训练不改（p4 式(6)，p6 Implementation details） | 采样 | 融合 / 其它：同模型采样多样性塌缩（我方 8 个采样余弦 0.998，两种子平均只 +0.005）。早期噪声可能拉开样本，给同模型集成或逐像素 max 读出留出空间。注意：没有 score 修正的噪声注入会改变边缘分布，单样本精度可能下降 | 否 | 否（不是引导，也不是换源分布） | Tab.3 p7：w/o annealed noise 为 SID 1.791 / FD 5.010 / P-FD 1.027；完整模型 4.062 / 2.805 / 0.587。注意：这个消融没报告 Div（单输入多次采样之间的距离）和 MSE。SID 也不等于单输入多样性，Tab.1 p6 中确定性 UniTalker 的 SID 为 2.21、Div 为 0.00，所以既证明不了“打破同模型采样塌缩”，也看不出对单样本精度的影响 |
| P5 Motion Memory：冻结 VQ 原型的后期微调正则（只作用于有效倾听窗口） | 先只在有效倾听窗口的 GT 片段上预训练 VQ tokenizer 和 codebook（codebook 512，code dim 128，hidden 256）并冻结。基础训练 1000 epoch 收敛后，短暂微调 120 epoch（lr 1e-4，λp=0.0015），损失为 L_proto=Σw_k‖Enc(Ỹ_k)−sg(Q(Enc(Y_k)))‖1/(Σw_k+ε)。Ỹ_k 从融合代理预测 ỹ 上裁取；只量化 GT 片段，生成片段保持连续；按窗口活跃度加权（p5 式(12)(14)，p6）。迁移设想：在训练集强对流、新生或增强片段上建原型库，只在这些窗口上约束生成 | 训练损失（后期微调） | 高阈值欠报：弱条件窗口大致对应新生/增强事件（类比，没有证据） | 否（codebook 只用训练集 GT，推理期不检索） | 是：已关闭族：x̂0 上的加权/感知/拓扑损失（本质是在一步外推的融合预测上算冻结编码器特征 L1）；另有加重记忆化的风险 | Tab.3 p7：w/o Motion Memory 为 SID 3.669 / FD 3.257 / P-FD 0.708；完整模型 4.062 / 2.805 / 0.587 |
| P6 非对称条件路由：锚分支取中间层特征，残差分支取深层特征，副流都做低通 | 共享 HuBERT 编码器：确定性锚分支取中间层表征 f_s，加低通后的副流 c_o=P(f_o)；随机残差分支取更深、更上下文化的表征 f_s^d，副流同样低通 c_o^d=P(f_o^d)。P(·) 是非参数的时间下采样再上采样（p3–p4 式(2)(3)，p4 §4.2） | 条件 / 结构 | 其它：条件设计。我方输入只有 5 帧单层雷达，没有副流，能迁移的只剩“锚取浅层、残差取深层”的特征路由，与我方问题对得不准 | 是：原文共享骨干是外部预训练 HuBERT；路由思想本身在协议内 | 是（仅预训练 HuBERT 部分）：已关闭族：外部基础模型先验；路由本身不属任何已关闭族 | 无单独消融。Tab.3 p7 只有把副流整条去掉的 w/o counterpart audio（SID 3.531 / FD 3.588 / P-FD 0.835），不能归因到低通或层路由 |

- **撞车**：无（融合线（仅叙事层面相似：确定性与随机互补）；条件流匹配线只共享 rectified-flow 底座）— ECHO 在面部 FLAME 动作上联合训练“确定性锚 + 残差流匹配 + 可学习分组缩放”，论证只用确定性或只用随机都不如两者结合（Fig.1 叙事，Tab.3 两个分解消融）。这与我方“跨范式融合是唯一显著杠杆”方向一致，但实现和领域都不同：它是单模型联合训练的加性残差（与 DiffCast 同构，属已关闭族“确定/残差分解”）；我方是两个独立训练模型做像素级事后融合，并有逐事件互补证据（r=0.91，各赢一半）。它没有雷达、没有 CSI，同域先例本来就是 DiffCast，不构成撞车。只有在我方采用 P2（分组可学习 α）时，才需要把 ECHO 作为跨领域先例引用。与强度阈值阶梯线、UOT 质量损失线、SDIR 载体线无关。
- **值得上机**：1/3 — 领域在外（面部 FLAME 系数），指标是 FD/SID/P-FD 这类分布/覆盖指标，没有 CSI；主干分解属已关闭族，且与 DiffCast 同构。能直接搬的只有两个零训练代价的旋钮：(1) 分组可学习的融合系数 α（P2），是均值融合的推广，需在训练集留出子集上拟合，但原文没有“可学习 α vs 固定 α”的单独对比；(2) 推理期早期退火噪声（P4），Tab.3 有单独消融，但只报告了 SID/FD/P-FD，没报告 Div 和 MSE，不能证明它拉开了单输入采样，也不知道单样本精度的代价。两者都在协议内，几小时就能测完，但没有任何证据表明能涨 CSI-M。
- **是否引用/比较 SDIR**：不适用
- **核查记录（11 条，节选前 3 条）**：大部分数字核对无误。我打开 2609.05506 v1（10 页，arXiv 标注 29 Aug 2026 [cs.GR]），把第 6、7 页渲染成图，对照 Tab.1/Tab.3 排版逐行逐列核对。Tab.3 七行、Tab.1 的 MSE 列和 ProbTalk3D 对比数字、第 6 页 α/噪声/Motion Memory 超参都与原文一致。Tab.3 单位确认如下：FD ×10⁻²，P-FD ×10⁻³，局部 P-FD 下颌 ×10⁻³，局部 P-FD 颈部 ×10⁻⁴。 ／ key_numbers 第 11 条标签有误：原写“与纯确定性基线对比”，但所列 ProbTalk3D 在 Tab.1 中属于 Stochastic models；DualTalk† 额外用了对方动作输入，是更强输入的参考。标签已改正。 ／ P4 的证据链有缺口：Tab.3 的“w/o annealed noise”只报告 SID/FD/P-FD，没报告 Div（每个输入多次采样之间的两两距离），也没报告 MSE。而且 SID 不是单输入采样多样性：Tab.1 里确定性模型 UniTalker 的 SID 为 2.21、DualTalk† 为 2.03，Div 都是 0.00。所以“SID 4.062→1.791”不能直接当成“退火噪声打破了同模型采样塌缩”的证据，也看不出它对单样本精度的影响。已补进 P4 的 ablation_evidence 和 score_reason。

---
<a id="p260602453"></a>
### Initialization is Half the Battle: Generating Diverse Images from a Guidance Potential Posterior

- **ID**：arXiv 2606.02453　**Venue**：arXiv 预印本（v1，PDF 首页注记为“Preprint. June 2, 2026.”，未见录用信息）。GitHub README 自述“ICML 2026 Spotlight”，未能在 PDF 或官方渠道核实
- **Venue 依据**：PDF 第1页原文“Preprint. June 2, 2026.”，arXiv 戳记“arXiv:2606.02453v1 [cs.CV] 1 Jun 2026”，全文 grep 不到 ICML、Accepted 或 Spotlight。github.com/South7X/divin 的 README（HEAD 7c57c1e）标题行写“[ICML 2026 Spotlight]”，且 README 里的标题是“...Diverse Initialization for Mitigating Memorization in Diffusion Models”，与 arXiv 标题不同。WebSearch 只找到第三方笔记站 papernotes.org 把它列在 ICML2026 类下，不是官方来源。录用信息只能算自述。
- **代码**：有代码（已 git clone 核实，含实现；本地 HEAD 7c57c1e 与远端一致）。divin/pipelines/pipeline_sd.py 和 pipeline_sd3.py 实现了 Langevin 初始化：pipeline_sd3.py 第326行、pipeline_sd.py 第559行为 `lat_updated = lat - eta_lr * (grad * temperature + lat) + sigma_langevin * noise`。势函数在代码里是平方范数 `v_vec.norm(dim=1) ** 2`（sd3 第309行、sd 第542行），与论文 Eq.5 的不平方 L2 范数不一致。另有 generate.py、generate_imagenet.py 和 evaluation/（图像指标）。图像任务，没有 CSI 评估代码。（https://github.com/South7X/divin）
- **阅读层级**：全文PDF　**核查结论**：corrected　**任务**：F
- **一句话**：面向图像扩散/流匹配模型的推理期方法（DivIn）。默认只在噪声端 t=T 做 K=1 步 Langevin：x_T ← x_T − η(τ∇U + x_T) + √(2η)ξ。其中 U=／／x̂0(x_T,c) − x̂0(x_T,∅)／／_2，是条件与无条件 Tweedie 预测之差；论文 Eq.5 为不平方 L2 范数，官方代码用平方。目的是把初始噪声推向“引导势”低的平坦区，缓解模式坍缩、提高样本多样性。核心算子属于已关闭族（换源分布，且依赖 CFG 的条件/无条件差）。全文没有任何雷达或 CSI 实验。
- **CSI 口径**：数据集=非雷达。(1) 类生图：SD v1.4，用 ImageNet-1K 类名模板提示“a photo of a {class name}”生成 1 万张，以 ImageNet 验证集为参考；(2) 文生图：SD3.5 Medium（Rectified Flow），500 条混合通用提示（MS-COCO/Lexica/Tuxemon/GPT-4），每条提示生成 4 张；帧=不适用（非时序）；分辨率=256×256（SD1.4 ImageNet）/ 512×512（SD3.5）；刻度=不适用（RGB 图像，潜空间）；阈值=不适用：无 CSI。指标为 Recall/Vendi/Coverage/Precision/Density/FID/FD_DINOv2/Similarity/CLIP/Aesthetic/ImageReward；聚合=不适用：无 CSI。原文说图像指标“tracked per class/prompt and averaged”（Sec.5.1 p6）；出处=Sec.5.1 p5-6；Tab.1/Tab.2 p6；App.C p15-16（SD1.4 用 DDIM 50 步、CFG 7.5；SD3.5 用 Euler 30 步、guidance 7.0）

| 零件 | 去壳算子 | 插到我方 | 对准问题 | 越协议 | 已关闭族 | 单独消融证据 |
|---|---|---|---|---|---|---|
| P1 DivIn：初始噪声的引导势后验 Langevin 重采样 | p_diverse(x_T／c) ∝ exp(−τU(x_T,c))·N(0,I)，U=／／x̂0(x_T,c)−x̂0(x_T,∅)／／_2（Eq.5 为不平方范数，代码用平方）。默认只在 t=T 做 K=1 步 Langevin：x_T ← x_T − η(τ∇U + x_T) + √(2η)ξ。每步额外代价是噪声端一次前向（条件加无条件）和一次反向，NFE 从 50 变为 51（Fig.9 左表 p8）。之后的采样轨迹不变 | 采样 | 其它：同模型采样坍缩（我方 8 次采样余弦 0.998，两种子平均只 +0.005），间接对应融合 | 否（训练免调、推理期；但需要无条件分支，即条件 dropout 训练） | 是：换源分布（把 N(0,I) 换成依赖条件的后验），而且势函数依赖条件与无条件分支之差，属 CFG 及各类引导族 | Tab.1 p6（SD1.4 ImageNet，5 个种子均值）：Base → +DivIn，Recall 0.503→0.569，Vendi 4.265→4.688，FID 16.696→16.158，Precision 0.833→0.825。Tab.2 p6（SD3.5）：Vendi 1.803→1.864，CLIP 34.04→34.01。没有 CSI 类证据 |
| P2 Langevin 噪声项加高斯先验回拉（对比确定性种子优化） | 改初始噪声时保留两项：先验回拉 −ηx 和注入噪声 √(2η)ξ，不做 SAIL 式确定性梯度下降。原文 Fig.8 p8 显示：去掉噪声项，潜变量范数塌缩；去掉先验项，范数爆炸 | 采样 | 其它：以后若做任何测试期初始噪声优化，用它防止离流形伪影 | 否 | 是：换源分布（P1 的附属组件） | Tab.3 p8：完整 DivIn 的 Recall/Vendi/FID 为 0.569/4.688/16.158；去掉噪声项为 0.541/4.534/16.544；去掉先验项为 0.557/4.584/16.121；SAIL 为 0.543/4.549/16.395。K=3 时 FID 为：去噪声项 17.53、去先验项 17.36、完整 15.98（Sec.5.4 正文 p8） |
| P3 球面范数投影 x←√d·x/／／x／／ | 每次更新初始噪声后，把它投影回半径 √d 的超球面，强制保持高斯范数（App.D.4 的可选步骤，不属于默认 DivIn） | 采样 | 其它：初始噪声被修改后的离流形问题 | 否 | 是：换源分布（附属组件） | Tab.9 p19：DivIn 的 Vendi/Precision 从 4.699/0.825 变为 +proj 后的 4.679/0.835；SAIL 从 4.549/0.825 变为 +proj 后的 4.456/0.828。注意：Tab.9 中 DivIn 的 Vendi 4.699 与 Tab.1/Tab.3 的 4.688 不一致，是原文内部差异 |
| P4 在 x̂0（Tweedie）空间定义测试期能量，而非在 score/velocity 空间 | 把测试期能量投影到数据空间 x̂0（扩散用 Tweedie；FM 用 x̂0 = x_t − t·v），吸收与 t 相关的尺度 λ_t，使温度超参不受采样步数影响 | 采样 | 其它：采样步数变化时超参的鲁棒性（通用工程技巧）。按我方推断，FM 起点 t=1 与步数无关，这个问题在 FlowCast 上基本不存在，移植价值很低 | 否 | 否（技巧本身不在关闭族内，但原文用它服务于已关闭的换源分布族） | Tab.10 p19（SD1.4 扩散模型，不是 FM）：Tweedie 势取 τ=0.6，在 30 步和 50 步的 Recall/FID 分别为 0.585/16.28、0.569/16.16；score 势需要 τ′ 在 30 步取 130.7、50 步取 171.5，才得到 0.585/16.29、0.565/16.22 |
| P5 条件锐度（引导势 U）作为逐条件（逐事件）坍缩/记忆化诊断量 | 对每个测试事件，在噪声端对多个初始噪声计算 U=／／x̂0(x_T,c)−x̂0(x_T,∅)／／ 并取平均，衡量条件把轨迹拉向单一模式的强度。原文按提示层面显示，平均 U 越高，该提示的样本多样性越低；所引 SAIL（Jeon et al. 2025）把同类的条件锐度与记忆化联系起来。可移植为：按 U 给测试事件分层，看记忆化或欠报是否集中在高 U 事件 | 测试期 | 记忆化 / 测试漂移（只做诊断，不直接涨 CSI） | 否 | 否（纯诊断）。但前提是有无条件分支（条件 dropout 训练），我方 FlowCast 是否具备未核实；没有的话要换成对输入扰动的 x̂0 敏感度 | 无单独消融。只有提示层面的相关性：Fig.4 p4（1000 条提示，每条 10 个初始噪声；模型未明写，按 1000 类推测为 SD1.4），Spearman ρ=−0.4，p<0.001；Fig.11 p15（SD3.5 通用提示）ρ=−0.40 |
| P6 问题表述：条件 Hessian 迹决定生成熵衰减（采样坍缩的理论解释） | Theorem A.3（Eq.20 p14，针对 VP 扩散的 PF-ODE 推导）：dH_t/dt = −(d/2)β_t + (1/2)β_t·E[tr(H_c)]。条件锐度越大，概率体积收缩越快，不同噪声种子会合并到同一输出 | 融合 | 融合（叙事支撑，属记录者引申，原文没有此论证：同模型采样几乎一样，可归因于条件流收缩强，从而解释为什么增益只来自跨范式融合的去相关误差）。原文定理针对 VP 扩散，推广到 FM 未经证明 | 否 | 否（只是叙事与理论） | 无 CSI 证据。只有玩具实验 Fig.3 p3（9 模式 2D GMM，解析 score，CFG 7.5）：标准初始化找回 5 个模式，DivIn（K=2）找回全部 9 个 |

- **撞车**：无（条件流匹配临近预报线（只沾边：都属于 FM 采样的初始化环节））— 这篇是通用图像生成的多样性方法，不做雷达、不做时序、不算 CSI，也不涉及像素级融合、强度阈值阶梯、UOT 质量损失或 SDIR。唯一的交集是 FlowCast 也是条件流匹配，理论上可以换初始噪声，但那属于已关闭的换源分布族。而且它追求的是提高多样性，和我方以 CSI 为目标的方向相反（均值预报要的是准，不是散）。
- **值得上机**：0/3 — 核心算子（P1-P3）属于已关闭族：换源分布，外加依赖 CFG 无条件分支。优化目标是图像多样性而非 CSI，全文没有雷达或 CSI 证据。协议内能移植的只有 P5 诊断量和 P6 叙事；P6 用于我方融合的引申是记录者自己的推断，P4 对 FM 起点基本没有意义。这些都不会直接涨 CSI，所以不值得上机。
- **是否引用/比较 SDIR**：不适用
- **核查记录（9 条，节选前 3 条）**：U 的定义写错了。原文 Eq.5（p4，渲染图确认下标是 2 而非平方）、Algorithm 1 第7行（p5）、App.A.1（p12）都把 U 定义为不平方的 L2 范数 ／／x̂0(x_T,c)−x̂0(x_T,∅)／／_2；记录在 one_liner 和 P1 里写成了平方范数。官方代码 pipeline_sd.py 第542行、pipeline_sd3.py 第309行用的是 `v_vec.norm(dim=1) ** 2`，即平方范数，论文和代码不一致。已改为同时写明两者。 ／ Fig.4（p4）只写了“1000 distinct prompts”，没写用的是哪个模型。记录里的“SD1.4”只是从 1000 类 ImageNet 提示推出来的，已标为推测。Fig.11（p15）明写是 SD3.5，ρ=−0.40，这一条核对无误。 ／ P5 写成“逐样本诊断量”不准确。原文 Fig.4 的相关性是按提示（条件）算的：每条提示取 10 个初始噪声，平均势对 10 张样本的 Vendi 分数。已改为“逐条件（逐事件）”。
